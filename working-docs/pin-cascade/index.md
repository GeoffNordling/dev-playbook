---
okf_version: "0.1"
---

# working-docs/pin-cascade/ — index

The working documentation set for the pin cascade — the system that
moves every governed repo's dev-playbook pin to the published head when
`main` moves, works each repo to green, and records the run. Start at
the root.

- [Pin Cascade](/working-docs/pin-cascade/ROOT.md) — The working root for the pin cascade — the goal, the decisions the user has ruled on, the design of the cascade command and its ledger, the timer, the headless per-repo run, and the worklist; speculative until the cascade has run once over the governed repos
