---
okf_version: "0.1"
---

# working-docs/pin-updates/ — index

The working documentation set for the pin updates — the system that
moves every governed repo's dev-playbook pin to the published head when
`main` moves, works each repo to green, and records the run. Start at
the root.

- [Pin Updates](/working-docs/pin-updates/ROOT.md) — The working root for the pin updates — the goal, the decisions the user has ruled on, the design of the update-pins command and its ledger, the timer, the headless per-repo run, and the worklist; speculative until update-pins has run once over the governed repos
