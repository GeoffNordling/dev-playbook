# working-docs/doc-type-system/doc-type-system/ — index

The doc-type system strand: the language of doc-types, verbs, and
predicates, and the user's own words on it, kept verbatim. Start at
the root.

Ordering: the root, then reading order.

- [Doc-Type System](/working-docs/doc-type-system/doc-type-system/ROOT.md) — The root of the doc-type system strand — the language of doc-types, verbs, and predicates, its principles and terms, and the worklist from the Standard's new shape through the first instance
- [Reference Model](/working-docs/doc-type-system/doc-type-system/reference-model.md) — The doc-type system's target state as a reference model — three DocTypes, ten verbs, the parts each composes, and how they fit, in pseudocode, then where each thing the picture has no place for goes
- [Writing Predicates](/working-docs/doc-type-system/doc-type-system/writing-predicates.md) — How to write a rule so a verifier can decide it — one member, one moment, one bool, the property not the witness, the file it reads named, what not how, the future not the past, no new nouns, the trailer, and what is not a predicate
- [Body Drain](/working-docs/doc-type-system/doc-type-system/body-drain.md) — The prompt one agent loads to drain one ruleset — the target shape of a rule, the id and kind it proposes, the five-verdict rubric for the body, the detector check, the exactness a deterministic predicate keeps, the edit it makes, and the report it returns
- [Ontology Solvers](/working-docs/doc-type-system/doc-type-system/ontology-solvers.md) — Ontology solvers against the doc-type system — the fact base as an ABox, the doc-types as a TBox, a Standard's rules as shapes, a type checker as the same solver over program text, the route from the fenced pseudocode to real Python, and the document and domain ontologies another repository splits into, none of it decided
- [Personal Notes](/working-docs/doc-type-system/doc-type-system/personal-notes.md) — The user's own words on the doc-type system and predicates, kept verbatim for use outside this working set
- [rule-audit/](/working-docs/doc-type-system/doc-type-system/rule-audit/index.md) — Step 10's rule audit: the shared prompt an agent loads to audit one Standard family's rules, and the reports each family produces
