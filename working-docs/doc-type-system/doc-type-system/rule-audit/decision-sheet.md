---
type: General-Sheet
title: Rule Audit Decision Sheet
description: The rulings step 11 needs from the seventeen rule-audit reports — every reclassification, low-value rule, break in this repo, and weak check on one line each, with the auditor's proposal, the reviewer's verdict, and the user's ruling
---

# Rule audit: decision sheet

One row per item the seventeen reports under `rule-audit/` escalate,
built from their tables and Proposal paragraphs. The `verdict` column
is filled by a reviewer who checked the auditor's proposal against the
Standard and the code; the user rules from this sheet. Breaks whose
evidence lives in another repository or on GitHub are listed last and
are out of scope: this pass fixes this repo's Standards against this
repo's state.

## Reclassify (10)

The trailer says one kind; the auditor says the predicate earns the other.

| id | rule | kind → proposed | auditor note | verdict |
|---|---|---|---|---|
| `standard.read-only` | A detector leaves everything git tracks as it found it. | deterministic → stochastic | Proving that arbitrary code never writes needs judgment. `verifier_table.py:449` and `boundary_table.py:345` write the two tables under `--write`. | **agree** — `ruff-format`, an address the table names (verifiers.yaml:195), rewrites tracked files; deciding arbitrary code never writes is run-time behavior, not a repo-file read |
| `standard.an-absent-surface-is-clean` | A detector whose surface is optional, a `skills/`, `standards/`, or `loops/` tree, exits 0 and reports no finding in a repo that has no such surface. | deterministic → stochastic | Which surface is optional, and what a detector does where it is absent, both need judgment. `standards_lint.py:649` returns early; `loop_lint.py:31` and `scripts/README.md:77` state the same for the other two. | **agree** — Predicate is run-time exit code and output in a repo lacking the surface; standards_lint.py:644 returns early, but no repo file decides another detector's behavior |
| `build.one-version-set` | Every version the canonical artifacts pin, the Python interpreter, ruff, mypy, pytest, and each hook `rev`, is the latest stable release. | deterministic → stochastic | "Latest stable release" is upstream state; no repo file holds it. Cannot be decided here. See Escalations. | **agree** — "Latest stable release" is upstream state on PyPI and GitHub; bump_pins.py moves only a consumer's dev-playbook rev, and no repo file holds those versions |
| `decisions.date` | A Decision Record's `date` frontmatter key holds the day the decision was made, written `YYYY-MM-DD`, or holds `null` where that day is unrecoverable. | deterministic → stochastic | No file states the day a decision was made, so a script cannot decide the predicate. All twenty-nine carry a `date`; three hold `null` (0002, 0005, 0006). | **agree** — No repo file records the day a decision was made, and "unrecoverable" is a judgment; 3 of 29 records hold null (0002, 0005, 0006) |
| `distribution.the-roster` | A governed repo is named in workspace-lint's `GOVERNED` roster, and every name in the roster is a repo under the workspace root. | deterministic → stochastic | `GOVERNED` names `lunch` and `date-tree` (`src/dev_playbook/workspace_lint.py:169-170`); neither is under the workspace root, so `workspace_repos` raises and the audit exits 2. | **agree** — Second clause stats other repos' checkouts, outside this repo: workspace_lint.py:169-170 names lunch and date-tree, neither under ~/workspace; which repos are governed is judgment |
| `knowledge-organization.stable-named-anchor` | A reference's `#anchor` names its target heading by the words of the heading and carries no number that is the heading's position in the file. | deterministic → stochastic | No anchor in the repo starts with a digit. The second sentence, "names the concept … in the link text", needs judgment. | **agree** — Second sentence, "names the concept the heading carries in the link text" (cross-references.md:46-48), is a judgment; the first sentence is near-vacuous |
| `knowledge-organization.workspace-path-for-a-stable-location` | A reference to a file at a stable location in the referencing file's own repository is an inline link whose target is the full `~/workspace/<repo>/<path>` path, unless the target is inside the referencing file's own skill bundle. | deterministic → stochastic | 28 `~/.claude/…` links name in-repo files by their stowed path. "Stable location" is the complement of a stochastic rule's judgment. | **agree** — "Stable location" is the complement of stochastic inline-code-for-a-varying-location (cross-references.md:110-113); 34 `~/.claude/…` links show the judgment is live |
| `modules.results-are-returned-not-written` | A module that computes a value returns it and does not mutate the caller's argument to deliver it. | stochastic → deterministic | Test over every `.py` under `src/`: a function that mutates a name bound by its own signature, `self` and `cls` aside. `_directory_node` fills the caller's `reached` set. | **disagree** — stochastic: the "to deliver it" clause and the "doing its work" carve-out (design.md:78-80) are judgments; AST finds one site, index_tree.py:142, an accumulator by design ⇒ stochastic |
| `testing.the-mocking-library` | A mock in the suite is built with `unittest.mock`. | deterministic → stochastic | `unittest.mock` is imported nowhere in the suite, yet `tests/test_transcript_export_client.py:34` hand-rolls a recording double. Deciding the rule first needs a judgment on which doubles are mocks. See the escalation. | **agree** — Which doubles are mocks is the judgment of stochastic testing.mocks-at-boundaries-only; recording_runner at test_transcript_export_client.py:34 sits on that line |
| `tracking.artifacts` | Where a build leaf's body carries an `Artifacts` section, each block under it sits in a code fence, four backticks when the block has fences of its own. | deterministic → stochastic | "Block" is undefined, so which prose under the heading must be fenced is a judgment: #452 fences its clause, #383 quotes with `>`. The four-backtick clause alone is deterministic. | **agree** — "Block" is undefined, so which prose must be fenced is a judgment; the population is a GitHub issue (issue-shapes.md:5), not a repo file |

## Low value (28)

Default ruling: delete. Strike a row to keep the rule.

| id | rule | state | overlap | auditor note | verdict |
|---|---|---|---|---|---|
| `standard.a-first-party-detector` | The detector is a script the audited repo hosts at `scripts/<name>`. | breaks | none | A condition written as an obligation. False of the six dependency addresses the verifier table names, `mypy`, `ruff-check`, `ruff-format`, `shellcheck`, `shfmt`, `pre-commit validate-manifest`. | **agree** — Condition, not obligation: `### Thin shims` and six more H3 rules sit under it. Delete the trailer and the standards/verifiers.yaml row; the sentence stays as the condition. |
| `build.javascript` | A repo in which `package.json` exists at the root. | holds | none | Condition, vacuously true here: the only `package.json` is at `src/dev_playbook/cloa_viewer/web/`, so no governed repo enters the layer. | **amend** — Condition: `### Lockfile committed` sits under it, repo-lint's `infer_layers` reads root `package.json`, and standards/build/explanation.md:24 links the anchor. ⇒ Drop the trailer `build.javascript` · deterministic and its standards/verifiers.yaml row; keep the heading and its sentence as the condition gating lockfile-committed |
| `build.lockfile-committed` | A lockfile, `package-lock.json`, `pnpm-lock.yaml`, `yarn.lock`, `bun.lock`, or `bun.lockb`, is tracked beside `package.json`. | holds | none | Vacuous: no member is in the JavaScript layer. The repo's real JS project sits inside the Python package, where the rule never reaches. | **disagree** — keep: scripts/repo-lint:64 emits the id and tests/test_repo_lint.py:558 asserts it, so deleting the heading breaks `standard.an-emitted-id-is-a-rule-heading`. |
| `build.artifactsmk` | `artifacts.mk`, where it exists at the root, sets `ARTIFACTS` to a list of files and gives each one a rule whose target is that file's path. | holds | none | Verifier row is null. `artifacts.mk` sets `ARTIFACTS := $(WEB)/dist/index.html` and gives that path a rule. `make` already fails loud on a missing rule. | **disagree** — keep: `-include artifacts.mk` at Makefile:3 is silent when absent, and make never catches a .PHONY target in ARTIFACTS, the fault standards/build/explanation.md:137 names. |
| `decisions.slug-case` | A Decision Record's slug, the part of its filename after `NNNN-` and before `.md`, is kebab-case. | holds | `decisions.sequential-numbering` | Test: match `^\d{4}-[a-z0-9]+(-[a-z0-9]+)*\.md$`. All twenty-nine slugs pass. Sequential-numbering already states the filename shape but not the slug's case. | **agree** — All 29 slugs pass, decisions_lint.py:63 matches `^(\d+)-.+\.md$` and never fires on case, the verifier row is null, and no document links the anchor. |
| `decisions.template` | A Decision Record's frontmatter holds `type: Decision-Record`, a `title`, a `description`, and a `date`; its body opens with an H1 repeating the `title`, followed by one to three sentences: | breaks | `knowledge-organization.types` | Twenty-one records miss the one-to-three-sentence opener: twelve open straight on `## Context`, nine run four to eleven sentences. See the escalation. | **disagree** — rewrite: delete loses the only statement of the frontmatter contract; only the one-to-three-sentence clause breaks, on the 12 records opening straight on `## Context`. ⇒ A Decision Record's frontmatter holds `type: Decision-Record`, a `title`, a `description`, and a `date`; its body opens with an H1 repeating the `title`. Drop the sentence-count line from the fenced template. |
| `decisions.optional-sections` | A Decision Record's body carries no section beyond the H1 except `Considered Options` and `Consequences`, and neither of those sections is empty. | breaks | `decisions.template` | Twenty of twenty-nine carry other H2s — `Context`, `Decision`, `Why`, `Alternatives considered`, `Recovery`, `Addendum`. See the escalation. | **amend** — Same row as above: `decisions.immutable-after-merge` forbids the repo-change, so delete is the only way out, and the explanation's link to the anchor must go with it. ⇒ Delete the rule and drop the ([Optional sections](/standards/decisions/records.md#optional-sections)) link at standards/decisions/explanation.md:101 so ref-lint stays clean |
| `distribution.a-pinned-rev` | A governed repo's `.pre-commit-config.yaml` pins the hook repository to a `rev`, unless the repo is the hook repository itself. | holds | `build.pre-commit-configyaml` | workspace-lint `check_pin` exempts the hook repo by identity and flags a missing config or pin. Seven roster consumers carry a pin; `lunch` and `date-tree` are not on disk to check. | **disagree** — keep: workspace_lint.py:85 declares the id and lines 404-407 emit it, so deleting the heading breaks `standard.an-emitted-id-is-a-rule-heading`. |
| `distribution.the-roster` | A governed repo is named in workspace-lint's `GOVERNED` roster, and every name in the roster is a repo under the workspace root. | breaks | none | `GOVERNED` names `lunch` and `date-tree` (`src/dev_playbook/workspace_lint.py:169-170`); neither is under the workspace root, so `workspace_repos` raises and the audit exits 2. | **disagree** — repo-change: the false roster names are the fault, not the rule. `workspace_repos` raises and the audit exits 2 today, which its docstring calls a false claim by the roster. ⇒ Drop "lunch" and "date-tree" from GOVERNED at src/dev_playbook/workspace_lint.py:169-170; neither directory exists under /home/geoff/workspace |
| `doc-type.one-sentence` | `definition.md` opens with one sentence that says what one instance is and what the doc-type does. | holds | none | Judgment: whether a sentence says what the doc-type does is taste. The file's frontmatter `description` and its H1 section already tell a reader what an instance is. | **disagree** — rewrite: nothing else requires `definition.md` to open with a definition. Only the "what the doc-type does" clause is taste; all three definition.md files pass the rest. ⇒ `definition.md` opens with one sentence that says what one instance is. |
| `doc-type.disallowed-tools-restate-nothing` | A skill's `disallowed-tools`, when present, names no tool or call that a `settings.json` beside its skills root already denies. | holds | none | Vacuous: no skill carries `disallowed-tools`, and `dotfiles/dot-claude/settings.json` denies one call, `AskUserQuestion`. It forbids a redundancy nobody has committed. | **agree** — No skill carries `disallowed-tools` and dotfiles/dot-claude/settings.json:49-51 denies only AskUserQuestion; no document links the anchor, explanation.md:128 links `#tool-fields`. |
| `doc-type.skillmd-at-most-500-lines` | A skill's `SKILL.md` body is at most 500 lines. | holds | none | `body_length_advisory` prints to stderr and emits no rule id, which `standards/harness/explanation.md:39-42` states is deliberate. The longest body is `ralph-setup` at 144 lines. | **disagree** — keep: the 500 bound is authored only here. scripts/harness-files-lint:116 hardcodes BODY_MAX_LINES and standards/harness/explanation.md:39-42 cites the bound as the advisory's source. |
| `knowledge-organization.resource-names-the-asset` | A concept document's `resource`, where present, names the asset the document describes, never a companion file that only supports it. | holds | none | 3 members: the OKF spec's upstream URL and two workflow `.js` files. The explanation already carries the worked example. | **disagree** — keep: both recipe docs point `resource` at a workflow `.js` that has a companion skill, and explanation.md:92-95 names that exact substitution as the fault. |
| `knowledge-organization.no-tags-or-timestamp` | A concept document's frontmatter has no `tags` key and no `timestamp` key. | holds | none | No member carries either key and nothing in the repo writes frontmatter. 29 records carry `date`, 30 Standards carry `population`; neither is banned. | **disagree** — keep: docs/references/okf-spec.md:137-138 offers `tags` and `timestamp` as optional OKF keys, so the rule is this repo's declared divergence, not an idle ban. |
| `knowledge-organization.global-table` | The declaration is the `## Types` table of `standards/knowledge-organization/document-types.md`, in the repo that carries `standards/build/canonical/`. | holds | none | okf-lint raises and exits 2 on an unusable registry rather than emitting this id. The lead paragraph of type-registry.md already states the same fact. | **amend** — Condition: `### Row shape`, `### Row description` and `### Alphabetical order` are its H3 children, and sibling `local-declaration` at line 55 has the same shape. ⇒ Drop the trailer `knowledge-organization.global-table` · deterministic and its standards/verifiers.yaml row; keep the heading and sentence as the condition |
| `knowledge-organization.fixed-repo-root` | The referencing file has a fixed repo root: no segment of its path inside the repository is `skills`, `rules`, or `agents`. | holds | `knowledge-organization.no-fixed-repo-root` | A condition, not an obligation: no member can fail it. The heading must stay; its trailer and verifier row carry nothing. | **agree** — `### Link, same bundle` sits under it, so it is a condition per `doc-type.the-rule-shape`. The heading stays; the trailer and the verifier row go. |
| `knowledge-organization.no-fixed-repo-root` | The referencing file has no fixed repo root: a segment of its path inside the repository is `skills`, `rules`, or `agents`. | holds | `knowledge-organization.fixed-repo-root` | The exact complement of `fixed-repo-root`; the two cover every file and neither can fail. Same verdict on the trailer. | **agree** — Three H3 children make it a condition, and standards/knowledge-organization/explanation.md:71 links the anchor, so the heading must stay and only the trailer goes. |
| `knowledge-organization.terms-defined-once` | A term is defined once, in the member whose concern it is, and every other use links that definition. | holds | `knowledge-organization.one-home` | The family's explanation says a definition is a fact like any other, so `one-home` subsumes this. Strand roots link the set root's Terms rather than redefine. | **agree** — standards/knowledge-organization/explanation.md:132 says a definition is a fact like any other, so its home follows one-home. No document links the anchor. |
| `knowledge-organization.working-docs-holds-only-sets` | `working-docs/` at the repo root, a plain parent to the sets [an index in every directory](/standards/knowledge-organization/documentation-sets/documentation-sets.md#an-index-in-every-directory) makes, holds its own `index.md` and the directories of the sets, and nothing else. | holds | `knowledge-organization.where-a-set-lives` | `working-docs/` holds `index.md`, `doc-type-system/`, `software-factory/` and nothing else. Binds one directory against a fault nobody commits. | **disagree** — keep: `where-a-set-lives` binds a set, not its parent, so only this rule bars a stray file at `working-docs/` — a live fault in an agent-written tree. |
| `modules.two-adapters-or-no-seam` | A seam a module presents carries at least two adapters, concrete things that satisfy the interface at that seam; a test's in-memory fake is one. | holds | `modules.internal-seams-stay-inside` | Every seam found carries two: `Kind.generate`, `runner`, `ListRules`. Counting a test fake as the second adapter means any seam with a test passes, so no member can break it. | **disagree** — keep: a seam driven only through its one real adapter, with no fake, still fails it. standards/modules/explanation.md:92-94 cites it as the single-adapter indirection test. |
| `modules.the-interface-is-the-test-surface` | Every behaviour of a module is reachable through its interface. | breaks | `testing.access-only-public-names` | What counts as a behaviour needs judgment. 36 `monkeypatch.setattr` calls reach behaviour no interface offers. The overlap states the test-side half, is deterministic, and is checked. | **disagree** — keep: the overlap is weaker than claimed. testing_lint.py:186-197 flags only private names, and the 36 setattr calls mostly patch module constants, not behaviour. |
| `prose.point-at-canonical-artifacts` | Where a file is itself the standard, the document references that file and does not restate its contents. | breaks | `knowledge-organization.one-home` subsumes it | `standards/prose/explanation.md:99-101` restates `.prose-lint-vocabulary`'s entry rather than linking the file. | **disagree** — rewrite: `one-home` permits a linked restatement and this rule forbids restating, so it is not subsumed; the absolute sentence breaks at standards/prose/explanation.md:103-106. ⇒ Where a file is itself the standard, the document links that file rather than reproducing its contents; naming one entry as a worked example is not reproduction. |
| `shell.dialect-directive` | A sourced fragment opens with a `# shellcheck shell=bash` directive. | holds | `shell.bash-declared` | Derivable: bash-declared forces a shebang or the directive, no-shebang-no-strict-mode forbids the shebang. The only increment is "opens with", the line-1 position. | **agree** — shellcheck SC2148 already fails a fragment with no top directive, and `bash-declared` plus `no-shebang-no-strict-mode` force it. All three dotfiles/.bashrc.d files carry it on line 1. |
| `testing.pytest` | A governed repo's Python test suite runs on pytest. | holds | `build.pyprojecttoml` | Test: `pyproject.toml` carries `[tool.pytest.ini_options]` and a `pytest` floor in `[dependency-groups] dev`. Both present. `build.pyprojecttoml` pins `tool.pytest.ini_options.testpaths` and the canonical `pytest>=9.0` floor, so it subsumes this. | **amend** — Delete stands: standards/build/canonical/pyproject.toml:11 and :17 pin `tool.pytest.ini_options.testpaths` and `pytest>=9.0`, both enforced by repo-lint. The frontmatter goes stale. ⇒ Delete the rule and drop "the pytest framework, " from the `description` key in standards/testing/conventions.md frontmatter |
| `testing.test-file-naming` | Every test file in the suite is named `test_*.py`. | holds | none | True by construction: the `population` defines a test file as a `test_*.py` file, so no member can falsify it. A script could not find the counterexample, because the counterexample is not a member. | **agree** — The population phrase already reads "the test_*.py files anywhere in its tree", so no member can falsify it and no document links the anchor. |
| `testing.the-mocking-library` | A mock in the suite is built with `unittest.mock`. | breaks | none | `unittest.mock` is imported nowhere in the suite, yet `tests/test_transcript_export_client.py:34` hand-rolls a recording double. Deciding the rule first needs a judgment on which doubles are mocks. See the escalation. | **disagree** — keep: it does not break. `unittest.mock` is absent from tests/ and `recording_runner` at test_transcript_export_client.py:34 is a fake by `the-lightest-double`. The trailer is the error. ⇒ `testing.the-mocking-library` · stochastic |
| `tracking.prohibited-surfaces` | A build leaf's `Prohibited surfaces` names only the paths whose touching is a real hazard. | holds | none | Whether a path is a real hazard is a judgment. The sampled entries name real ones, such as dev-playbook#444's `dotfiles/dot-claude/skills/frozen-*`. Binds the taste of one optional list. | **agree** — The template line at standards/tracking/issue-shapes.md:98, pinned by the deterministic `tracking.build-headings`, already reads "why this issue must not touch it". The rest is taste. |
| `tracking.standing-rulings` | Where an epic's body carries a `Standing rulings` heading, the rulings under it are a numbered list. | holds | none | Test: the first block after the heading is an ordered list. Holds on #437, #256, and #378, the three epics that carry the heading. Binds the formatting of one optional section. | **agree** — Binds list formatting inside one optional epic section; the three epics that carry it all pass and the verifier row is null. No reader or script loses anything. |

## Breaks in this repo (72)

`proposal` is the auditor's way out; `detail` is the new sentence for a rewrite, the files for a repo change, or what still states the intent for a delete.

| id | rule | value | proposal | detail | evidence | verdict |
|---|---|---|---|---|---|---|
| `standard.read-only` | A detector leaves everything git tracks as it found it. | high | rewrite | A detector leaves everything git tracks as it found it, in the mode a gate runs it in; a second, explicitly requested write mode, `shfmt -w` or `verifier-table --write`, is enforcement and not a detector run. | scripts/verifier-table --write and boundary-table --write modify tracked files (src/dev_playbook/verifier_table.py:449), breaking the read-only predicate for two detectors. | **amend** — The shfmt hook's args are `-w` and ruff-format rewrites files at the commit gate, so the gate-mode clause is false; a run-time predicate is stochastic. ⇒ A first-party detector run without an explicit write flag leaves everything git tracks as it found it; `verifier-table --write` and `boundary-table --write` are enforcement, and a dependency the verifier table names may write at its gate, `ruff-format` and `shfmt -w`. Kind: stochastic. |
| `standard.a-first-party-detector` | The detector is a script the audited repo hosts at `scripts/<name>`. | low | delete | the sentence stays on the page as the subset's definition | Six of 19 addresses in standards/verifiers.yaml (mypy, ruff-check, ruff-format, shellcheck, shfmt, pre-commit validate-manifest) are dependencies, not scripts/<name>, falsifying the predicate. | **agree** — Condition, not obligation: `### Thin shims` and six more H3 rules sit under it. Delete the trailer and the standards/verifiers.yaml row; the sentence stays as the condition. |
| `standard.thin-shims` | The script holds no rule logic of its own: it puts the host repo's package on the import path and calls that package's entry point. | high | repo-change | scripts/okf-lint, scripts/repo-lint, scripts/harness-files-lint, scripts/ref-lint, scripts/python-lint, each paired with a new module under src/dev_playbook/ and its tests under tests/ | okf-lint (842 lines), repo-lint (801), harness-files-lint (668), ref-lint (287), python-lint (182) define own rule logic instead of calling src/dev_playbook/. | **amend** — scripts/README.md and build/explanation.md:189 plainly want shims, but moving 2780 lines from five scripts into new modules plus tests is no small change ⇒ Track the five moves as a CANDIDATES.md entry, not this PR; the rule's sentence stands |
| `standard.the-hosting-pattern` | A first-party detector is published as a hook in its repo's `.pre-commit-hooks.yaml` and has a row in the validation table of `scripts/README.md` where the repo has that file. | high | rewrite | A first-party detector is reachable from its repo's published hook — named in the `playbook-lint` roster, or published as its own hook in `.pre-commit-hooks.yaml` — and has a row in the validation table of `scripts/README.md` where the repo has that file. | .pre-commit-hooks.yaml publishes only playbook-lint, not detectors themselves; workspace-lint listed only in Utility table, not validation table. | **amend** — `workspace-lint` is a registered ungated audit (playbook_lint.py:60), in neither the roster nor the manifest, and rowed in the Utility table, not the validation table. ⇒ A first-party detector is reachable from its repo's published hook — named in the `playbook-lint` roster, or published as its own hook in `.pre-commit-hooks.yaml` — or is a registered ungated audit, and has a row in a table of `scripts/README.md` where the repo has that file. |
| `standard.offered-by-the-canonical-template` | A first-party detector in the repo that carries `standards/build/canonical/` is a hook of that canonical `.pre-commit-config.yaml`'s pinned block. | high | rewrite | In the repo that carries `standards/build/canonical/`, the canonical `.pre-commit-config.yaml`'s pinned dev-playbook block offers exactly the hook ids `.pre-commit-hooks.yaml` publishes. | standards/build/canonical/.pre-commit-config.yaml pins only playbook-lint; the other 12 first-party detectors are never hooks of that block, by design. | **amend** — The sentence binds the canonical config, not a population member, and this H3 sits under the condition `A first-party detector`; standards_lint.py:569-570 compares manifest ids with canonical ids. ⇒ A first-party detector is a hook of the canonical `.pre-commit-config.yaml`'s pinned dev-playbook block exactly where `.pre-commit-hooks.yaml` publishes it as a hook, in the repo that carries `standards/build/canonical/`. |
| `standard.directory-layout` | Every immediate subdirectory of `standards/` is a Standard directory: it holds at least one file typed `Standard`, and every other `.md` file under it, `index.md` aside, is typed `Standard` or `Explanation`; the only flat files under `standards/` are `README.md` and `index.md`; standards-lint reports a departure. | high | rewrite | …; the only flat files under `standards/` are `README.md`, `index.md`, `verifiers.yaml`, and `boundaries.yaml`; standards-lint reports a departure. | standards/ holds four flat files, not two; verifiers.yaml and boundaries.yaml unlisted although this Standard requires them there. | **amend** — `_flat_strays` (standards_lint.py:196-205) bans only flat `.md` files, so the two yaml tables are already tolerated; naming them pins two files the detector never reads. ⇒ Every immediate subdirectory of `standards/` is a Standard directory: it holds at least one file typed `Standard`, and every other `.md` file under it, `index.md` aside, is typed `Standard` or `Explanation`; the only flat `.md` files under `standards/` are `README.md` and `index.md`; standards-lint reports a departure. |
| `build.name-mapping` | The root `pyproject.toml` sets `project.name` to the repo directory's name lowercased, `My-Repo` → `my-repo`, and the import package is that name with each hyphen an underscore, `my_repo`. | high | rewrite | The root `pyproject.toml` sets `project.name` to the repository's own name lowercased, the directory holding the shared `.git` and therefore the same from the main checkout and from every worktree, `My-Repo` → `my-repo`, and the import package is that name with each hyphen an underscore, `my_repo`. | pyproject.toml sets name to dev-playbook, not the worktree directory name; detector uses canonical_repo_name instead of directory name. | **agree** — repo-lint:776 reads `gitrepo.canonical_repo_name`, the `--git-common-dir` parent, so the sentence matches the code and holds from this worktree, where `project.name` is `dev-playbook`. |
| `build.entry-points` | `[project.scripts]` in the root `pyproject.toml` is absent, or every entry under it has the value `<package>.cli:main`, where `<package>` is the import package and `src/<package>/cli.py` defines `main`. | high | rewrite | `[project.scripts]` in the root `pyproject.toml` is absent, or every entry under it has the value `<module>:main`, where `<module>` is a module inside the import package and that module defines `main`. | pyproject.toml entry cloa-viewer points to dev_playbook.cloa_viewer.cli:main, not dev_playbook.cli:main; src/dev_playbook/cli.py absent; rule untested, missing from repo-lint RULES. | **agree** — `cloa-viewer = "dev_playbook.cloa_viewer.cli:main"` and cloa_viewer/cli.py:120 defines `main`, so the loosened form is true; verifiers.yaml:8 maps the rule to null, so no code changes. |
| `decisions.template` | A Decision Record's frontmatter holds `type: Decision-Record`, a `title`, a `description`, and a `date`; its body opens with an H1 repeating the `title`, followed by one to three sentences: | low | rewrite/delete | A Decision Record's frontmatter holds `type: Decision-Record`, and its body opens with an H1 repeating the `title`. | Twelve records have no opening sentence and nine run past three; `docs/decisions/0003-decline-superpowers.md` has none between H1 and H2. | **amend** — The sentence is true — all 29 records carry `type: Decision-Record` and an H1 repeating `title` — but the md block still shows `{1-3 sentences}` and the three dropped keys. ⇒ Keep the auditor's sentence and cut the template block to the `type: Decision-Record` frontmatter line and the H1, since `{1-3 sentences}`, `title`, `description`, and `date` leave the predicate. |
| `decisions.context-decision-and-reason` | A Decision Record's opening sentences give the context the decision was made in, the decision itself, and the reason for it. | high | rewrite | A Decision Record's body gives the context the decision was made in, the decision itself, and the reason for it. | Twelve records have no opening sentences; `0005-issue-workflow-reorganization.md` puts context, decision, and reason in separate H2 sections instead. | **agree** — 0003 and 0005 give context, decision, and reason in H2 sections, so `body` is right; note `decisions.optional-sections` still bars the `## Context` sections this relies on. |
| `decisions.immutable-after-merge` | A Decision Record that `main` carries has its body, and every frontmatter key other than `status`, byte-identical to the first commit on `main` that carries the file. | high | rewrite | A Decision Record that `main` carries has the substance of its decision unchanged from the first commit on `main` that carries the file; only its `status`, its frontmatter, and mechanical citation repairs change thereafter. | Sixteen of 29 records differ from their first commit; `0011-one-registry-for-non-authored-content.md` gained a `date` frontmatter line post-merge. | **amend** — Commit d56e671 conformed ADR terminology across nine records — not status, frontmatter, or a citation repair — and judging substance unchanged is a judgment call. ⇒ A Decision Record that `main` carries has the substance of its decision — its context, its choice, and its reason — unchanged from the first commit on `main` that carries the file; its `status`, its frontmatter, its citations, and its terminology may be conformed thereafter. Kind: stochastic. |
| `decisions.optional-sections` | A Decision Record's body carries no section beyond the H1 except `Considered Options` and `Consequences`, and neither of those sections is empty. | low | delete | `decisions.template`'s opener, the prose standard's brevity rules, and explanation.md: "The value is in recording that a decision was made and why, not in filling out sections." | Twenty of 29 records carry forbidden sections; `0001-adopt-matt-pocock-conventions.md` carries five extra H2 headings including an Addendum. | **amend** — Same row as above: `decisions.immutable-after-merge` forbids the repo-change, so delete is the only way out, and the explanation's link to the anchor must go with it. ⇒ Delete the rule and drop the ([Optional sections](/standards/decisions/records.md#optional-sections)) link at standards/decisions/explanation.md:101 so ref-lint stays clean |
| `decisions.external-convention-evaluation` | A Decision Record's decision is a verdict on something outside the workspace: a skill, a skill collection, a framework, or a technique. | high | rewrite | A Decision Record whose decision is a verdict on something outside the workspace — a skill, a skill collection, a framework, or a technique — is bound by the rules below. | Twenty-one of 29 records decide something inside the workspace; `0009-same-repo-resolution.md` sets a path-resolution rule, not an outside verdict. | **amend** — encoding.md:60-64 says a condition's first paragraph states the membership test, and the H3 nesting already binds the rules, so `is bound by the rules below` restates the mark. ⇒ A Decision Record whose decision is a verdict on something outside the workspace: a skill, a skill collection, a framework, or a technique. |
| `decisions.what-was-examined` | The Decision Record names the source and pins the exact state examined: the repository SHA, the release or version, and the date it was read. | high | rewrite | The Decision Record names the source and pins at least one of the exact state examined: the repository SHA, the release or version, or the date it was read. | Four of eight records pin nothing; `0003-decline-superpowers.md` names a bare repository URL with no SHA, release, or date. | **amend** — Under `at least one`, 0001, 0003, 0004, 0015, 0016, 0020, 0026 pin a date or a version; only 0006 pins nothing, carrying `date: null` and naming no SHA or release. ⇒ Keep `at least one of`, and let the record's own `date` serve as the date read; then `0006-harvest-pocock-prototype-and-handoff.md` is the one record to fix, by a pin or a date. |
| `doc-type.one-base` | `contract-shape.md` holds one class that extends `DocType`, declaring `operations` and `frontmatter`; every other class in its block is nested inside that one, extends nothing, and declares no operations. | high | rewrite | "`contract-shape.md` holds one class that extends `DocType`, declaring `operations` and `frontmatter`; every other class in its block extends nothing and declares no operations, and a class that is a part of the doc-type is nested inside that base." | doc-types/standard/contract-shape.md's Finding class sits top-level, not nested, breaking the nesting clause of one-base. | **agree** — Finding sits top-level in doc-types/standard/contract-shape.md:67 but is what a verifier returns, not a part; Rule, Edge, Act, Check, and Yield all nest, so the sentence holds. |
| `doc-type.the-rule-shape` | Each rule of a Standard is a level-two heading, a first paragraph that is the predicate every member of the population is held to, at most one block or table stating the target state the predicate compares against, and last a trailer line, `` `<name>.<slug>` · deterministic `` or `` `<name>.<slug>` · stochastic ``, where `<name>` is the directory and `<slug>` the heading's GitHub slug; nothing follows the trailer before the next heading. | high | rewrite | "any number of blocks or tables stating the target state the predicate compares against"; "A level-three heading sits only under a level-two rule, and is then a rule bound under it"; "where `<name>` is the `standards/<name>/` directory the file sits under" | Three facts break it: two yaml blocks in standards/doc-type/runbook-conventions.md, H3s under rule H2s, and mismatched id prefixes in documentation-sets.md. | **amend** — standard-conventions.md already reads `a level-two heading that is a condition`, matching encoding.md:60-64; the auditor read a stale text and `rule` would revert commit 50d6950. ⇒ Take the first and third changes; drop the second — the sentence already reads "A level-three heading sits only under a level-two heading that is a condition, and is then a rule bound under that condition". |
| `doc-type.steps-end-on-a-completion-criterion` | Every step of a runbook's body ends on a completion criterion: the condition that tells the agent the work is done. | high | rewrite | Where a runbook's body is a numbered list of steps, each step ends on a completion criterion: the condition that tells the agent the work is done. | log-friction/SKILL.md:31-38 steps end on the action, not a criterion; only update-standards-pin/SKILL.md gives every step one. | **disagree** — delete: commit/SKILL.md:29-31 and log-friction/SKILL.md:31-38 are numbered `## Steps` lists whose steps end on actions, so the scoped rewrite stays false; one runbook complies. |
| `doc-type.carries-its-chain` | Every edge of a runbook's contract is declared in the runbook's own file: what it accepts by the front matter `arguments` list, and each read, write, banned write, do, override, and report as a span in the body that encoding.md parses, except a ban the span vocabulary cannot carry, which stays plain prose in the body and is listed in residual-ledger.md. | high | rewrite | except an edge the span vocabulary cannot carry | dotfiles/dot-claude/skills/research/SKILL.md:11 carries a does edge as plain prose, not a span; most ledger entries aren't bans. | **agree** — residual-ledger.md:33-58 lists reality probes, interview loops, and presentation gestures — not bans — and the research launch it could not span is entered at :206-208. |
| `doc-type.interactive-skills-inherit` | A skill that runs several turns with the user carries `model: inherit`. | high | repo-change | dotfiles/dot-claude/skills/runbook-creator/SKILL.md; dotfiles/dot-claude/skills/enable-repo-governance/SKILL.md; dotfiles/dot-claude/skills/update-standards-pin/SKILL.md | Three skills wait on the user mid-run but pin a model instead of inherit, e.g. dotfiles/dot-claude/skills/runbook-creator/SKILL.md:6 sets opus. | **agree** — doc-type/explanation.md:121 says a pin governs only the loading turn; runbook-creator interviews, enable-repo-governance:75 and update-standards-pin:127 wait; sixteen skills already inherit |
| `harness.members` | Every file the harness consumes is a member of the table below, with the class the table gives it. | high | repo-change | standards/harness/files.md | dotfiles/dot-claude/statusline.sh is missing from the members table; the workflows/*.js row is misspelled `.claude/workflows/*.js`. | **agree** — settings.json:204 runs dotfiles/dot-claude/statusline.sh, absent from the table; every other row is root-relative, so `.claude/workflows/*.js` should read `workflows/*.js` |
| `harness.location` | A skill is `<skills root>/<name>/SKILL.md` and an agent is `<agents root>/<name>.md`, the roots being `.claude/skills/` and `.claude/agents/`, and `dotfiles/dot-claude/skills/` and `dotfiles/dot-claude/agents/` where those directories exist. | high | rewrite | agents/           # optional: bundle metadata for another harness | Eight skill bundles (e.g. dotfiles/dot-claude/skills/research/agents/openai.yaml) carry an undocumented agents/ subdirectory the block omits. | **agree** — Eight bundles carry `agents/openai.yaml` — research, wayfinder, grilling, prototype and four more — holding `interface` display metadata for another harness, and the block omits it. |
| `harness.one-rule-per-heading` | Each rule of the global source is one `###` heading under its bucket: a dispositional stance under `## Principles`, an operating rule for a named situation under `## Behaviors`. | high | repo-change | dotfiles/dot-claude/CLAUDE.md | dotfiles/dot-claude/CLAUDE.md:77 puts `### Notice repeatable work`, an operating rule, under `## Principles` instead of `## Behaviors`. | **agree** — CLAUDE.md:79 reads "when the task at hand looks repeatable, say so" — an operating rule for a named situation, so the heading belongs under `## Behaviors` |
| `knowledge-organization.description-voice` | A concept document's `description` is a sentence fragment in the present tense that names what the document is or what it governs. | high | rewrite | A concept document's `description` is a sentence fragment in the present tense that names what the document is, what it governs, or, for a `Decision-Record`, the decision it records. | Decision Records' description names the decision, not what the document is; e.g. docs/decisions/0001-adopt-matt-pocock-conventions.md:4 | **agree** — Every record's `description` opens on a verb naming the decision — 0011 "Exclude non-authored content…", 0019 "Collapse the two-name convention…" — which the current sentence does not admit. |
| `knowledge-organization.the-opening-sentence` | The introduction of an `index.md` opens with a single sentence naming what the directory holds, in that directory's own vocabulary; where the listing's sole entry carries a `description` that already says what the directory holds, the sentence says what the directory is for instead. | high | repo-change | docs/writing-improvement-process/index.md and doc-types/runbook/index.md | docs/writing-improvement-process/index.md and doc-types/runbook/index.md open with sentences restating the path/name, not the subject. | **agree** — doc-types/standard/index.md and doc-types/loop/index.md both name their contents; runbook's "The Runbook doc-type." and writing-improvement-process's sentence restate the path |
| `knowledge-organization.citation-another-repo` | A reference to a file or directory in a repository other than the referencing file's own is an inline link whose target is the full workspace path, beginning `~/workspace/<repo>/`. | high | rewrite | A code block, fenced with triple backticks or `~~~` or indented four spaces, may hold `~/workspace/` and `/`-root paths in shell examples or sample output, and a reference inside it is out of scope. | log-friction/SKILL.md:45 uses an indented code block with bare ~/workspace/ paths; exemption covers only fenced blocks. | **amend** — The fenced-code exemption is the file's `population` frontmatter and preamble, not this predicate; md.py:155-166 skips only fences, so log-friction/SKILL.md:45's indented block is scanned. ⇒ Change the `population` phrase and the preamble, not this rule: `except inside a code block, fenced or indented` in the frontmatter, and the auditor's sentence with `or indented` in place of `or indented four spaces` in the preamble. |
| `knowledge-organization.skill-invocation` | A reference to a skill is its bare slash invocation, `/<skill-name>`, with no link and no code markup. | high | rewrite | A reference that tells the reader to invoke a skill is its bare slash invocation, `/<skill-name>`, with no link and no code markup; a reference to a skill's file is a Link or a Citation like any other file reference. | 23 sites across 11 files wrap skill invocations in links or code markup (e.g. compact-prep/SKILL.md:17); rule bars every mention. | **amend** — `{Run [/commit](~/.claude/skills/commit/SKILL.md)}` (compact-prep/SKILL.md:17) and code-marked invocations (harness-recipes/recipes/ralph-loop.md:38) are deliberate, so "no link and no code markup" stays false. ⇒ A reference to a skill names it by its slash invocation, `/<skill-name>`. |
| `knowledge-organization.workspace-path-for-a-stable-location` | A reference to a file at a stable location in the referencing file's own repository is an inline link whose target is the full `~/workspace/<repo>/<path>` path, unless the target is inside the referencing file's own skill bundle. | high | rewrite | A reference to a file at a stable location in the referencing file's own repository is an inline link whose target is the full `~/workspace/<repo>/<path>` path, or, where the target is a skill or an agent definition the harness loads from `~/.claude/`, that runtime path; the exception is a target inside the referencing file's own skill bundle. | 28 links use ~/.claude/… runtime paths instead of `~/workspace/dev-playbook/…` for in-repo agents/skills, e.g. doc-set-deslopper.md:70. | **agree** — doc-set-deslopper.md:70 links `~/.claude/agents/doc-set-auditor.md`; ref-lint:185-191 reads only `/` and `~/workspace` targets, so the runtime path costs no code change. |
| `knowledge-organization.inline-code-for-a-varying-location` | A reference to a file whose path varies between repositories, `CLAUDE.md`, `CONTEXT.md`, `specs/design.md`, or `Makefile`, or to a directory, `docs/decisions/`, is inline code. | high | rewrite | A reference that names a file generically, because its path varies between repositories — `CLAUDE.md`, `CONTEXT.md`, `specs/design.md`, `Makefile`, or the directory `docs/decisions/` — is inline code; a reference to one repository's own copy, such as an `index.md` listing row, is a Link. | index.md:13 links CONTEXT.md as required by The listing, but the rule demands inline code for varying-location files. | **disagree** — keep: repo-root `index.md` has a fixed repo root, so this H3 under `No fixed repo root` (cross-references.md:82, 106) never binds index.md:13; there is no conflict. |
| `knowledge-organization.no-agent-instructions-or-decisions` | A `README.md` holds no instruction addressed to an agent and no architecture decision. | high | rewrite | A `README.md` holds no instruction addressed to an agent and no hard-to-reverse decision of the kind a Decision Record carries. | dotfiles/README.md:33-52 states and argues a design decision about the settings symlink, with no Decision Record covering it. | **disagree** — repo-change: dotfiles/README.md:43-52 argues the settings design and rejects a named alternative, per-machine settings variants, which meets decisions.the-bar, so the narrowed sentence still fails there. ⇒ Record the settings-symlink design as a Decision Record and cut dotfiles/README.md:43-52 to a pointer; the auditor's sentence then stands. |
| `knowledge-organization.entry-shape` | An entry under `## Language` in a repo's `CONTEXT.md` is the term in bold on its own line, its definition on the lines beneath, and, at most, one final `_Avoid_:` line naming the words retired in the term's favor. | high | rewrite | Rewrite the block to match the file, dropping the colon after the bold term and showing one H3 group. | CONTEXT.md's 14 entries omit the template's trailing colon and use four H3 groups instead of flat entries under ## Language. | **agree** — CONTEXT.md:26 writes `**Audit**` with no colon and groups entries under `### Governance` at :21, so the block, not the predicate, is what departs from the file. |
| `knowledge-organization.tight-definitions` | An entry's definition in a repo's `CONTEXT.md` is one sentence that says what the term is, and where a concept document defines the term the definition links that document. | high | rewrite | An entry's definition in a repo's `CONTEXT.md` opens with one sentence that says what the term is, and may add at most one sentence that separates the term from a neighbouring term; where a concept document defines the term, the definition links that document. | Seven of 14 CONTEXT.md entries add a second contrast sentence (e.g. Lint at :30); rule limits definitions to one sentence. | **amend** — Four second sentences are not contrasts — `Verifier table` names its writer, `Concern` its use, `Procedure` its span, `Runbook` its invocation — so the neighbouring-term clause is false. ⇒ An entry's definition in a repo's `CONTEXT.md` is at most two sentences: one that says what the term is, and at most one more that sharpens it; where a concept document defines the term the definition links that document. |
| `knowledge-organization.one-home` | A fact, rule, or decision has one home, the member whose concern is the thing the fact binds and the most general such member where the fact still holds; every other document links there, whether it sits in the same set, another set, or another repo, and states the fact without its reason. | high | repo-change | standards/knowledge-organization/explanation.md | `standards/knowledge-organization/explanation.md:141-144` states two facts about working-docs and strands without linking to their true homes. | **agree** — Both facts have homes: `knowledge-organization.working-docs-holds-only-sets` and the strand definition at working-documentation-sets.md:21; the same paragraph already links "Where a set lives" |
| `knowledge-organization.terms-that-cross-sets` | A term the set coins and a document outside the set uses has an entry in the repo's `CONTEXT.md`. | high | repo-change | CONTEXT.md | `CONTEXT.md` lacks entries for strand, bucket, and child set, terms the set coins and outside members use bare. | **amend** — strand and bucket do cross (doc-types/doc-type.md, harness/claude-content.md:72), but "child set" is already covered by CONTEXT.md's Documentation set entry ⇒ CONTEXT.md entries for strand and bucket only |
| `knowledge-organization.speculative-voice` | Every member of a working documentation set writes a guess as a guess and sets an open question beside its topic, and the set's `ROOT.md` declares the set speculative. | high | rewrite | Every member of a working documentation set writes a guess as a guess, and the set's `ROOT.md` declares the set speculative and holds the set's open questions. | Only 4 of ~29 members carry an open question; `working-docs/software-factory/docs/tdd.md` states flat requirements with none held open. | **amend** — Dropping the per-member open question is right, but "ROOT.md holds the set's open questions" is false: `working-docs/software-factory/ROOT.md` holds none and `viewer/ROOT.md:13` puts them beside their topic. ⇒ Every member of a working documentation set writes a guess as a guess, and the set's `ROOT.md` declares the set speculative. |
| `knowledge-organization.the-link-tree` | Every member of a working documentation set is reached from the set's `ROOT.md` by a path of links, a second structure over [an index in every directory](/standards/knowledge-organization/documentation-sets/documentation-sets.md#an-index-in-every-directory)'s tree of sets. | high | repo-change | working-docs/software-factory/ROOT.md; working-docs/doc-type-system/doc-type-system/ROOT.md | 12 members unreached from ROOT.md: `working-docs/doc-type-system/doc-type-system/rule-audit/PROMPT.md` and all 11 software-factory/docs files, named as inline code not linked. | **amend** — software-factory/ROOT.md:15 freezes its members pending deletion; on the live side all 19 files under rule-audit/ are unreached, not PROMPT.md alone ⇒ working-docs/doc-type-system/doc-type-system/ROOT.md only, linking into rule-audit/ from its step-10 entry |
| `knowledge-organization.where-a-set-lives` | A working documentation set is one directory under `working-docs/` at the repo root, `working-docs/<work>/`, holding the set's `index.md`, its `ROOT.md`, and its members under lowercase kebab-case names, flat or in subdirectories — the one [an index in every directory](/standards/knowledge-organization/documentation-sets/documentation-sets.md#an-index-in-every-directory) makes a set, here named and placed. | high | rewrite | A working documentation set is one directory under `working-docs/` at the repo root, `working-docs/<work>/`, holding the set's `index.md`, its `ROOT.md`, and its members under lowercase kebab-case names, `README.md` and a runbook prompt at `PROMPT.md` excepted, flat or in subdirectories. | `working-docs/doc-type-system/doc-type-system/rule-audit/PROMPT.md` and `software-factory/docs/README.md` are capitalized members the kebab-case clause doesn't exempt. | **amend** — Exemption list is short: ten `SKILL.md` under `working-docs/software-factory/skills/` and `tests/test_launcher.py`, `code/factory/__init__.py` are also non-kebab members. ⇒ A working documentation set is one directory under `working-docs/` at the repo root, `working-docs/<work>/`, holding the set's `index.md`, its `ROOT.md`, and its members under lowercase kebab-case names, flat or in subdirectories, except a member whose kind fixes its name: `README.md`, `PROMPT.md`, `SKILL.md`, and a Python file, named as Python names files. |
| `knowledge-organization.a-set-stands-on-main` | A working documentation set, the one [an index in every directory](/standards/knowledge-organization/documentation-sets/documentation-sets.md#an-index-in-every-directory) makes a set, is present on `main`. | high | rewrite | A working documentation set sits under `working-docs/` on every branch its work touches, and stays there when the work's branch merges rather than being deleted or moved under `docs/`. | `working-docs/software-factory/` isn't on main; created by commit 329a9b5 on this branch, unmerged, so the predicate can't hold yet. | **disagree** — delete — "on every branch its work touches" and "stays there when the branch merges" are lifecycle claims, not true of one checkout at one moment; what remains restates `where-a-set-lives`. |
| `knowledge-organization.buckets` | Every fact in a member of a working documentation set sits under a named section, its bucket ([one home](/standards/knowledge-organization/documentation-sets/documentation-sets.md#one-home) made navigable), and a bucket holds facts of its own type only. | high | rewrite | Every fact in a member of a working documentation set sits under a named section, its bucket, except the member's lead paragraph, which says what the member is and what it is for. | `working-docs/software-factory/docs/tdd.md` lines 9-26 define terms and requirements before any heading, breaking the bucket-only-facts clause. | **amend** — The lead-paragraph exemption is right, but it silently drops "a bucket holds facts of its own type only", and `docs/tdd.md` lines 9-26 define terms, not purpose. ⇒ Every fact in a member of a working documentation set sits under a named section, its bucket, and a bucket holds facts of its own type only; the section under the member's H1, which says what the member is and what it is for, is exempt. |
| `knowledge-organization.terms` | A term coined by the work and used in more than one member of a working documentation set is defined in the `Terms` bucket of one `ROOT.md`: the root of the smallest strand that holds every member using the term, and the set's own root where the term crosses strands, in place of the entry [terms that cross sets](/standards/knowledge-organization/documentation-sets/documentation-sets.md#terms-that-cross-sets) puts in the repo's `CONTEXT.md`. | high | repo-change | working-docs/software-factory/ROOT.md | `working-docs/software-factory/ROOT.md` has no Terms bucket; the frozen set already uses seam and chunk across members. | **disagree** — keep — seam is coined by standards/modules/explanation.md:72 (Feathers), not by the work, and software-factory/ROOT.md:15 freezes the set pending deletion |
| `knowledge-organization.acronyms` | Every member of a working documentation set ends with an `Acronyms` appendix, holding a bare `None.` where the member uses no acronym, and an acronym is defined in the appendix of the highest member that uses it and in no other member's, in place of a definition above first use ([declare before use](/standards/prose/conventions.md#declare-before-use)). | high | repo-change | working-docs/doc-type-system/doc-type-system/body-drain.md; working-docs/doc-type-system/doc-type-system/personal-notes.md; working-docs/doc-type-system/doc-type-system/rule-audit/PROMPT.md | 14 members lack an Acronyms appendix; `working-docs/doc-type-system/doc-type-system/body-drain.md` is one of three doc-type-system oversights, software-factory left frozen. | **amend** — Right fix, wrong list: every one of the 19 files under rule-audit/ lacks the appendix, not PROMPT.md alone ⇒ body-drain.md, personal-notes.md, and all 19 files under rule-audit/ |
| `modules.deep-not-shallow` | A module's interface is small against the behaviour behind it: what a caller or a test must learn to use the module is little against the amount of behaviour that use reaches. | high | repo-change | src/dev_playbook/cloa_viewer/registry.py; tests/dev_playbook/cloa_viewer/test_registry.py | `kind_by_name` in src/dev_playbook/cloa_viewer/registry.py:22 has no caller outside its own test; external.py has one caller only. | **agree** — kind_by_name is exported at registry.py:17 but called only by tests/dev_playbook/cloa_viewer/test_registry.py; deleting it and its test shrinks the interface for free |
| `modules.internal-seams-stay-inside` | A module's interface exposes no seam — a place where behaviour is altered without editing in that place — that exists only for the module's own tests. | high | rewrite | A module's interface exposes a seam only where that seam is the module's port at a process boundary; every other seam the module's own tests reach stays inside the implementation. | src/dev_playbook/transcript_export/client.py:60's `runner` seam and verifier_table.py's `list_rules` seam serve only their own tests. | **agree** — Every interface seam in `src/` is a subprocess or daemon transport: `runner`, `list_rules` (verifier_table.py:320), `render` (cli.py:172), `render_subagent` (render.py:174). |
| `modules.dependencies-are-accepted-not-constructed` | A module takes each of its dependencies as a parameter and constructs none of them in its own body. | high | rewrite | A module constructs none of its dependencies in its own body: a dependency whose behaviour a caller or a test must vary is a parameter, and one that never varies is imported. | Every module reaches collaborators by import, not parameters; src/dev_playbook/gitrepo.py:105 builds its own inline git ls-files call. | **agree** — The gloss states what the repo does: collaborators by import, `runner`/`list_rules` as parameters; no module constructs an injected collaborator in its body. |
| `modules.a-port-at-a-process-boundary` | A dependency a module reaches across a process boundary, a service the workspace owns or a third party it does not control, sits behind a port at the module's own interface: the module holds the logic, and the transport that carries the call out of the process satisfies that port. | high | rewrite | A dependency a module reaches over the network, a service the workspace owns or a third party it does not control, sits behind a port at the module's own interface: the module holds the logic, and the transport that carries the call out of the process satisfies that port. | src/dev_playbook/gitrepo.py:38 and four other modules call git with no port; playbook_lint.py:94 spawns detector processes unfronted. | **amend** — "Over the network" is still false today: `workspace_lint.py:438` `_gh_json` reaches GitHub through `gh` with no port at any interface. ⇒ A module that holds logic reaches a dependency out of its own process only through a port, a module or function whose one job is that transport, and the transport carries the call itself. |
| `modules.the-interface-is-the-test-surface` | Every behaviour of a module is reachable through its interface. | low | delete | the instruction to redraw the interface rather than reach past it, in Module Design Explanation | 36 monkeypatch.setattr calls in tests/ bypass interfaces; src/dev_playbook/testing_lint.py:187 misses one because the name is a string. | **disagree** — keep: the overlap is weaker than claimed. testing_lint.py:186-197 flags only private names, and the 36 setattr calls mostly patch module constants, not behaviour. |
| `modules.results-are-returned-not-written` | A module that computes a value returns it and does not mutate the caller's argument to deliver it. | high | repo-change | src/dev_playbook/cloa_viewer/kinds/index_tree.py; src/dev_playbook/cloa_viewer/server.py; standards/verifiers.yaml | src/dev_playbook/cloa_viewer/kinds/index_tree.py:121's `_directory_node` and server.py's `rescan` mutate a caller argument to deliver a second result. | **amend** — index_tree.py:122 takes `reached` and :177 reads `tracked - reached`, a delivered result; rescan's app.state write is its stated job, which the rule's second sentence exempts ⇒ src/dev_playbook/cloa_viewer/kinds/index_tree.py only |
| `prose.one-rule-one-place` | Each rule the document states lives in the lead sentence of its section. | high | rewrite | "Each rule the document states lives in the lead sentence of its section, except a statement of the document's own scope, which sits in the section under the H1." | standards/prose/conventions.md:10-16 states three unbound rules with no heading, id, or verifier row; the pattern repeats repo-wide. | **agree** — `conventions.md:10-16` states the `.prose-lint-exempt` and `.prose-lint-vocabulary` scope rules under the H1; the exemption names exactly that case. |
| `prose.point-at-canonical-artifacts` | Where a file is itself the standard, the document references that file and does not restate its contents. | low | delete | `knowledge-organization.one-home` already states it and more: "A fact, rule, or decision has one home, the member whose concern is the thing the fact binds and the most general such member where the fact still holds; every other document links there." | standards/prose/explanation.md:99-101 restates the vocabulary file's values in prose instead of linking it; verifier row is null. | **disagree** — rewrite: `one-home` permits a linked restatement and this rule forbids restating, so it is not subsumed; the absolute sentence breaks at standards/prose/explanation.md:103-106. ⇒ Where a file is itself the standard, the document links that file rather than reproducing its contents; naming one entry as a worked example is not reproduction. |
| `prose.open-with-purpose` | The opening states what the document is for and what a reader should be able to do after reading. | high | rewrite | "The opening states what the document is for and what a reader should be able to do after reading; an `index.md` and a `README.md` answer instead to [The opening sentence](/standards/knowledge-organization/indexes.md#the-opening-sentence) and [The purpose sentence](/standards/knowledge-organization/readme-content.md#the-purpose-sentence)." | docs/index.md:3-4 and doc-types/index.md:7-10 say what the directory holds but not what a reader can do after reading. | **agree** — `docs/index.md:3-4` and `doc-types/index.md:7-10` state contents only; both linked anchors exist, `indexes.md:35` and `readme-content.md:24`. |
| `prose.block-form-fits-its-content` | A block's form fits what it holds: prose for an argument, a list for parallel items, a table for repeated structure, a callout for an aside, a quote for wording that is the point, and a code block for code that runs or spans lines. | high | rewrite | "The same shape with the same fields two or more times is a table; anything uneven is prose with bold leads." | Seven population files carry a two-row table, e.g. standards/tracking/explanation.md:142, breaking the three-or-more threshold. | **disagree** — keep — the predicate is sound; the defect is the target-state bullet at `conventions.md:69`, and the proposal throws away five of the six form pairs. ⇒ Keep the predicate verbatim; change the bullet to "**Table vs repeated structure.** The same shape with the same fields two or more times is a table; anything fewer or uneven is prose with bold leads." |
| `prose.declarative-present-tense` | Every sentence is in the present tense, except in a member of a working documentation set, which may write a guess as a guess. | high | rewrite | "Every sentence is in the present tense, except a sentence reporting a measurement or an incident that happened, and except in a member of a working documentation set, which may write a guess as a guess." | guides/headless.md:116,132,142 uses past tense to report a measurement; not a working-doc-set member so no exemption applies. | **agree** — Covers `guides/headless.md:116,132,142` and the merged Decision Records, whose past tense reports incidents, e.g. `0016-pocock-skills-sweep-2026-07.md:83`. |
| `prose.no-slop-tics` | The document commits none of the tics [Slop Tics](/guides/slop-tics.md) names. | high | repo-change | harness-recipes/README.md | harness-recipes/README.md:22 uses Flourish words "supremely-powerful" and "Alas" where plain words carry the same meaning. | **agree** — guides/slop-tics.md:118 defines Flourish as a dramatic word where an ordinary one carries the same meaning; README.md:22 "supremely-powerful" and "Alas" are both |
| `prose.third-person` | A declarative document speaks in the declarative mood and the third person, and does not address the reader as `you`, except inside a double-quoted utterance. | high | rewrite | "A declarative document speaks in the declarative mood and the third person, and does not address the reader as `you`, except inside a double-quoted utterance or where the document is a prompt an agent executes." | Seven files, e.g. guides/writing-for-agents.md:13, address the reader as "you" outside quotes; all are agent-facing instructions. | **disagree** — repo-change — no cited file is a prompt an agent executes; `docs/sandboxing.md:60` and `guides/slop-tics.md:252` are plain declarative prose, so the exemption misses its own evidence. ⇒ Rewrite the genuine uses to third person: `guides/writing-for-agents.md` (11 uses), `guides/slop-tics.md:252`, `docs/sandboxing.md:60`, `guides/bootstrap.md`, `guides/headless.md`, `working-docs/software-factory/docs/node-agent-and-skill-authoring.md`. The rest already sit inside quoted text. |
| `prose.name-concepts-once-use-consistently` | One name per concept holds across the document. | high | repo-change | standards/prose/explanation.md | standards/prose/explanation.md:96 and :99 name the same rule "banned-word" and "The banned word", three lines apart. | **disagree** — keep — explanation.md:96 names prose-lint's `banned-word` check; :99 "The banned word" names the synonym that check refuses. Two concepts, not one |
| `prose.terminology-the-person-is-the-user` | One actor — the dispatcher, reviewer, and approver — is the `user` throughout the document, its frontmatter, code spans, and fenced blocks included, never a synonym, in any case, plural, or compound. | high | rewrite | "A merged Decision Record is exempt" | Merged Decision Records 0016 and 0019 name the actor "owner"/"reviewer" instead of "user"; immutability rule bars fixing the repo. | **amend** — The detail is a clause, not a predicate, and the repo's own wording elsewhere (`conventions.md:35`) is "numbered", not "merged". ⇒ One actor — the dispatcher, reviewer, and approver — is the `user` throughout the document, its frontmatter, code spans, and fenced blocks included, never a synonym, in any case, plural, or compound. A numbered Decision Record is exempt. |
| `prose.the-banned-word` | No tracked file of the repo, except a path `.prose-lint-exempt` lists, contains the word [elided], bare or plural, in any case, alone or in a compound, its frontmatter, code spans, and fenced blocks included. | high | rewrite | "The document does not contain the banned actor noun, bare or plural, in any case, alone or in a compound, its frontmatter, code spans, and fenced blocks included." | docs/references/okf-spec.md is tracked, unlisted in .prose-lint-exempt, and contains the banned word at four lines; predicate also isn't per-member. | **amend** — Per-document scope is right — `okf-spec.md` is `type: Reference`, already outside the population, and prose_lint.py:44 already skips it — but naming no word leaves the ban undecidable. ⇒ Change only the scope, keeping the literal word `conventions.md:155` already names: "The document does not contain the word <that word>, bare or plural, in any case, alone or in a compound, its frontmatter, code spans, and fenced blocks included." |
| `prose.heading-casing` | The H1 is in Title Case and every heading below it is in sentence case, except that a proper noun or a code identifier keeps its native case at any level. | high | repo-change/rewrite | dotfiles/dot-claude/skills/wayfinder/SKILL.md, standards/decisions/records.md; add clause "A merged Decision Record is exempt" | wayfinder SKILL.md:24,78 and four merged records use Title Case headings; records.md:128 itself prescribes "Considered Options". | **amend** — `Considered Options` is the only Title Case heading in the four records, and records.md:128 prescribes it; exempt that name, not every merged record ⇒ The H1 is in Title Case and every heading below it is in sentence case, except that a proper noun, a code identifier, or a section name [Optional sections](/standards/decisions/records.md#optional-sections) prescribes keeps its native case at any level. |
| `prose.grammatical-parallelism` | Items that sit together take the same grammatical shape: the headings of a document, the bullets of a list, the clauses of a sentence. | high | repo-change | standards/prose/conventions.md; standards/verifiers.yaml; standards/prose/explanation.md | Four H2 headings in standards/prose/conventions.md, e.g. "Point at canonical artifacts", are imperative verb phrases, not noun phrases like the other sixteen. | **amend** — Renaming the four headings renames four rule ids; working-documentation-sets.md and agents/doc-set-deslopper.md must move too, and line 59 is a third shape ⇒ Add standards/knowledge-organization/documentation-sets/working-documentation-sets.md and dotfiles/dot-claude/agents/doc-set-deslopper.md to the files, and reshape `## Block form fits its content` as well |
| `python.docstrings` | Every module, class, function, and method in the file carries a docstring, except a file named `__init__.py` and a pytest test function, whose name begins with `test_`. | high | repo-change | pyproject.toml, scripts/, tests/, src/ | ruff's D exemptions cover tests/, private names, and extensionless scripts; 167 docstrings missing, e.g. scripts/python-lint:94 check_no_future | **disagree** — rewrite — pyproject.toml:63-69 exempts tests/ with a comment and ruff's D skips private names by design; 167 additions is not the small fix ⇒ Every module, class, function, and method in the file carries a docstring, except a file named `__init__.py`, a file under a `tests/` tree, and a name beginning with an underscore. |
| `python.fail-loudly` | A value the code requires is read directly, so a missing one raises. | high | repo-change | src/dev_playbook/testing_lint.py | `_add` at src/dev_playbook/testing_lint.py:209 defaults node.lineno to 0 instead of failing loudly on a missing line number | **agree** — Every `_add` caller (testing_lint.py:154, :193, :203) passes a stmt or expr node that carries lineno, so the getattr default papers over state the caller always supplies |
| `python.helper-justification` | Every helper function in the file is multi-use, substantial in body, a distinct concern at another abstraction level, or an entry in a dispatch table, registry, or strategy map: | high | rewrite | Every helper function in the file is multi-use, substantial in body, a distinct concern at another abstraction level, an entry in a dispatch table, registry, or strategy map, or a named seam its tests replace. | 105 single-call helpers of two statements or fewer go unjustified, e.g. scripts/repo-lint:204 canonical_raw mirrors canonical_text | **disagree** — keep — the seam clause misses its own evidence: `scripts/repo-lint:204` `canonical_raw` is no test seam. 534 trivial single-caller helpers repo-wide; no sentence closes that. |
| `python.helper-placement` | A helper function sits directly beneath the function that uses it, or, where two or more functions use it, in a `# ---` banner section. | high | rewrite | A helper function sits directly above the function that uses it, or, where two or more functions use it, in a `# ---` banner section. | 262 of 310 single-caller helpers sit above their caller; scripts/python-lint:94 check_no_future precedes scan_file, the opposite of the predicate | **agree** — Measured over `src/` and `scripts/`: 80 single-caller helpers sit directly above their caller against 19 below, e.g. `gitrepo.py:25` `_repository_local_env_vars` above `no_git_env`. |
| `python.annotated-signatures` | Every function and method in the file annotates each parameter, except a `self` or `cls` first parameter, and its return. | high | repo-change | Makefile, standards/build/canonical/Makefile, scripts/griffe-outline, scripts/harness-files-lint | make typecheck only runs mypy over src and tests; seven scripts/griffe-outline and scripts/harness-files-lint signatures go unchecked | **amend** — The seven signatures are real, but the canonical file is standards/build/canonical/Makefile.python, whose `<code-roots>` is a placeholder, and mypy cannot glob extensionless scripts ⇒ scripts/griffe-outline and scripts/harness-files-lint only |
| `shell.executable-scripts` | The shell file has the executable bit set. | high | rewrite | "A shell file with the executable bit set." | Predicate false for 4 of 9 shell files, e.g. dotfiles/.bashrc.d/aliases.sh, mode 644 despite being in the population. | **agree** — This H2 carries H3s, so it is a condition; the noun phrase matches the membership-test form `doc-types/standard/encoding.md:60-63` prescribes. |
| `shell.glue-only` | An executable script wires tools together and nothing more: it defines no function, declares no array, and reads no positional parameter. | high | rewrite | "An executable script wires tools together and nothing more: it declares no array and parses no options." | dotfiles/dot-claude/hooks/session-start-stale-base defines a function; dotfiles/dot-claude/hooks/play-sound reads $1 as a two-word event dispatch. | **agree** — True today: `session-start-stale-base` defines `note()`, `play-sound` dispatches on `${1:-}`, and no executable script declares an array or runs `getopts`. |
| `shell.sourced-fragments` | The shell file is under `.bashrc.d/`. | high | rewrite | "A shell file in a `.bashrc.d/` directory." | Predicate false for 6 of 9 shell files, e.g. dotfiles/dot-claude/statusline.sh, not under .bashrc.d/; path also written bare. | **agree** — Same condition case as `executable-scripts`: an H2 with H3s beneath it, so the noun phrase is the required form. |
| `shell.bounded-to-shell-integration` | A sourced fragment holds only what mutates the parent shell: directory changes, aliases, and completions. | high | rewrite | "A sourced fragment holds only what mutates the parent shell: directory changes, aliases, completions, and exported environment variables." | dotfiles/.bashrc.d/machine-env.sh exports SKIP, mutating the parent shell but not matching the closed list of directory/alias/completion. | **agree** — `dotfiles/.bashrc.d/machine-env.sh:9` exports `SKIP`; the other two fragments stay inside aliases, a `cd` function, and a completion. |
| `testing.conftest-hierarchy` | A fixture lives in the `conftest.py` of the narrowest directory whose tests use it. | high | rewrite/repo-change | A fixture more than one test file uses lives in the `conftest.py` of the narrowest directory whose tests use it; a fixture one test file uses lives in that file. / tests/conftest.py, tests/dev_playbook/conftest.py (new) | `ambient_git_dir` (tests/conftest.py:67) sits one level too high; `make_repo` (tests/conftest.py:94) has no users | **agree** — 30 fixtures already live in their one test module; `ambient_git_dir` serves five files all under `tests/dev_playbook/`, and no file takes the `make_repo` fixture. |
| `testing.one-fake-per-interface` | The suite holds one fake for an interface, and every test that doubles that interface uses it. | high | repo-change | tests/transcript_fakes.py, tests/test_transcript_export.py | Three fakes — `fake_daemon`, `list_runner`, `search_runner` (tests/transcript_fakes.py) — double the one `runner` interface; predicate wants one | **amend** — A fourth double, `refused_runner` at tests/test_transcript_export.py:51, fakes the same subprocess.run runner interface; the fold must take all four, not three ⇒ tests/transcript_fakes.py and tests/test_transcript_export.py, folding fake_daemon, list_runner, search_runner and refused_runner into one |
| `testing.the-mocking-library` | A mock in the suite is built with `unittest.mock`. | low | rewrite/delete | No test module imports a mocking library other than `unittest.mock`. / nothing | `recording_runner`, `refused_runner`, `boom_runner` (tests/test_transcript_export_client.py:34) are mocks by recorded-call assertion, yet no file imports `unittest.mock` | **disagree** — delete — no test module imports any mocking library, and "a mocking library other than `unittest.mock`" needs a judgment call about what counts, so it cannot be deterministic. |
| `testing.fixtures-for-setup-and-teardown` | Construction and cleanup go through pytest fixtures rather than ad-hoc setup code in a test body. | high | rewrite | Cleanup that must run after a test goes through a pytest fixture rather than code in a test body; construction may go through a fixture or a factory helper the test calls. | Suite builds via module-level `make_repo` (tests/test_repo_lint.py:37) called in each test body across nine modules, not the unused `conftest.py` fixture | **agree** — Nine modules define their own `make_repo` factory, and the cleanup that exists runs in fixtures, e.g. `tests/dev_playbook/cloa_viewer/test_cli.py:130`. |
| `tracking.entry-shape` | An entry is one list item: a bolded short name, an em dash, then one or two sentences of intent, with no fields, no acceptance criteria, and no checkboxes. | high | repo-change/rewrite | story-forge/CANDIDATES.md, dev-playbook/CANDIDATES.md:52; "no bolded field label and no `- [ ]` checkbox" | story-forge/CANDIDATES.md swaps hyphens for em dashes, drops the separator in places, and runs several entries past two sentences | **amend** — story-forge is another repo, out of this pass's scope; only CANDIDATES.md:51-58 breaks the two-sentence cap here, so the rule needs no loosening ⇒ CANDIDATES.md lines 51-58 only; the rule's sentence stands |

## Weak checks (28)

Not a ruling: the list of what each detector misses, carried into the detector design.

| id | rule | state | not checked | verdict |
|---|---|---|---|---|
| `standard.the-boundary-table` | A repo that asks any check, dev-playbook or a consumer whose `.pre-commit-config.yaml` wires a `scripts/` hook, carries `standards/boundaries.yaml`, byte-identical to what `scripts/boundary-table` writes: one row per address the verifier table names, sorted, each carrying the gates that run it in a fixed order drawn from `commit`, the pre-commit stage at `git commit`; `push`, the pre-push stage at `git push`, which runs `make check`; `ci`, a workflow under `.github/workflows/`; and `on-demand`, no gate. | holds | that boundaries.yaml's addresses equal verifiers.yaml's (rebuilt independently, not read back); a detector whose claimed ids are all rejected still gets a boundaries.yaml row without one in verifiers.yaml | **agree** — boundary_table.addresses() rebuilds from DETECTORS, UNGATED_AUDITS and DEPENDENCY_RULES and never reads verifiers.yaml, so a detector whose ids verifier_table rejects still gets a row |
| `standard.the-hosting-pattern` | A first-party detector is published as a hook in its repo's `.pre-commit-hooks.yaml` and has a row in the validation table of `scripts/README.md` where the repo has that file. | breaks | A first-party detector is reachable from its repo's published hook — named in the `playbook-lint` roster, or published as its own hook in `.pre-commit-hooks.yaml` — and has a row in the validation table of `scripts/README.md` where the repo has that file. | **amend** — workspace-lint is a first-party detector verifiers.yaml names, yet sits in no roster and no manifest, and its scripts/README.md row is in the Utility table ⇒ A first-party detector is reachable from its repo's published hook — named in the `playbook-lint` roster, wired as a `scripts/` hook its `.pre-commit-config.yaml` and `.pre-commit-hooks.yaml` both carry, or registered as an ungated audit — and has a row in a `scripts/README.md` script table where the repo has that file. |
| `standard.offered-by-the-canonical-template` | A first-party detector in the repo that carries `standards/build/canonical/` is a hook of that canonical `.pre-commit-config.yaml`'s pinned block. | breaks | In the repo that carries `standards/build/canonical/`, the canonical `.pre-commit-config.yaml`'s pinned dev-playbook block offers exactly the hook ids `.pre-commit-hooks.yaml` publishes. | **amend** — The sentence matches check_hook_surfaces' canonical leg, but this is an H3 under `standard.a-first-party-detector`, so its member must be a detector, not the template file ⇒ A first-party detector the repo carrying `standards/build/canonical/` publishes in `.pre-commit-hooks.yaml` is a hook of that canonical `.pre-commit-config.yaml`'s pinned dev-playbook block, which offers exactly the ids that manifest publishes. |
| `standard.directory-layout` | Every immediate subdirectory of `standards/` is a Standard directory: it holds at least one file typed `Standard`, and every other `.md` file under it, `index.md` aside, is typed `Standard` or `Explanation`; the only flat files under `standards/` are `README.md` and `index.md`; standards-lint reports a departure. | breaks | …; the only flat files under `standards/` are `README.md`, `index.md`, `verifiers.yaml`, and `boundaries.yaml`; standards-lint reports a departure. | **amend** — standards_lint._flat_strays reads flat `.md` only, so naming verifiers.yaml and boundaries.yaml leaves every other flat file undecided; scoping the clause to `.md` closes it ⇒ Every immediate subdirectory of `standards/` is a Standard directory: it holds at least one file typed `Standard`, and every other `.md` file under it, `index.md` aside, is typed `Standard` or `Explanation`; the only flat `.md` files under `standards/` are `README.md` and `index.md`; standards-lint reports a departure. |
| `build.pre-commit-configyaml` | `.pre-commit-config.yaml` holds every block of the canonical .pre-commit-config.yaml verbatim and in order; hooks may follow inside a block, further `repo:` blocks may sit between blocks, and the line pinning the dev-playbook `rev` carries the consumer's own value. | holds | `default_install_hook_types: [pre-commit, pre-push]`; `repos:` | **disagree** — full — config_segments splits at "  - repo:", so the preamble `default_install_hook_types` and `repos:` is segment 0 and find_run checks it; only the rev block is skipped, which the rule exempts |
| `build.the-source-directory` | Every file directly under `standards/build/canonical/` is one a rule of this Standard names, and every file a rule of this Standard names is directly under `standards/build/canonical/`. | holds | manifest doesn't learn of a rule added to canonical.md; manifest still demands a file whose rule is deleted from canonical.md | **agree** — CANONICAL_MANIFEST is a hard-coded frozenset in scripts/repo-lint; check_canonical_artifacts diffs the directory against that set, never against the rules of canonical.md |
| `build.name-mapping` | The root `pyproject.toml` sets `project.name` to the repo directory's name lowercased, `My-Repo` → `my-repo`, and the import package is that name with each hyphen an underscore, `my_repo`. | breaks | The root `pyproject.toml` sets `project.name` to the repository's own name lowercased, the directory holding the shared `.git` and therefore the same from the main checkout and from every worktree, `My-Repo` → `my-repo`, and the import package is that name with each hyphen an underscore, `my_repo`. | **agree** — gitrepo.canonical_repo_name returns the parent of `git rev-parse --git-common-dir`, so a worktree answers `dev-playbook`, not its own directory name; the rewrite states that |
| `doc-type.three-verb-sections` | After the graph, a document typed `Loop` holds three H2s, `Acts`, `Checks`, and `Yields`, in that order and no others, with nothing between the graph and the first of them. | holds | "a blank line between two separate markdown lists under the same H2 is invisible to it, and both lists read as one run of entries" | **agree** — entries_of skips blank lines (`if not line.strip(): continue`), so two lists under one H2 read as one run of entries; every other clause is checked |
| `doc-type.entries-point-and-condition` | Every entry of a document typed `Loop` states its condition, `fires when …` or `fires every iteration` for an act or a check and `yields when …` for a yield. | holds | "A second or third link in an act's or a check's entry — a pointer at a guide beside the runbook, say — is never resolved, so a dead link there passes" | **agree** — check_entry resolves only links[0] for an act or a check (a yield resolves every link), so a second link's target is never opened |
| `doc-type.references-one-level-deep` | No `.md` file in a skill's `references/` links to another `.md` file in that `references/`. | holds | skips links starting with ~ or /; only scans references/*.md, not subdirectories; LINK_RE matches inline links only, missing reference-style definitions | **agree** — check_references_depth skips targets starting `~` or `/`, globs `references/*.md` only, and LINK_RE matches inline `[t](x)` alone |
| `knowledge-organization.types` | A concept document's frontmatter has a `type` key whose value names one row of the table below, or one entry of the `okf_types` mapping in the frontmatter of the repo's own root `index.md` ([Type Registry](/standards/knowledge-organization/type-registry.md#local-declaration)). | holds | - | **disagree** — full — check_types flags a missing `type` and a value outside `registry`, which is the `## Types` table in apex mode and upstream plus okf_types names in consumer mode |
| `knowledge-organization.local-declaration` | The declaration is the `okf_types` mapping in the frontmatter of the root `index.md` of a repo that does not carry `standards/build/canonical/`, one entry per document type the repo declares for itself. | holds | Where the mapping lives; The apex exclusion; One entry per type | **agree** — The two LOCAL_DECLARATION sites flag a non-mapping okf_types and a legacy registry document; apex mode never reads okf_types, and no site counts entries per type |
| `knowledge-organization.the-listing` | An `index.md` lists, as a bullet holding a root-absolute markdown link and exactly once each, every concept document in its own directory and every child directory's own `index.md`, and lists nothing else; each concept document's entry carries that document's frontmatter `description` verbatim. | holds | exactly once each, for child indexes; lists nothing else, for a non-link item | **agree** — check_indexes Counters duplicates for concepts only and turns listed_children into a set; parse_index matches INDEX_BULLET, so a bullet with no root-absolute link is invisible |
| `knowledge-organization.ordering` | Within each group of an `index.md`'s listing, the concept documents and then the child-directory links, entries are in alphabetical order by link title, compared case-insensitively, and a `README.md` entry is the first entry of the whole listing; an introduction line beginning `Ordering:`, one before the first listed entry, releases the alphabetical order of both groups and never the `README.md` entry's place. | holds | the first entry of the whole listing; the group order itself | **agree** — check_index_ordering takes the README index within group_a, which holds concept bullets only, and sorts the two groups apart, never one against the other |
| `knowledge-organization.the-root-index` | The `index.md` at the repository root. | holds | - | **disagree** — full — the predicate only names the root `index.md`, and check_indexes emits THE_ROOT_INDEX exactly when `()` is no index directory, which is that file's absence |
| `knowledge-organization.reference-resolves` | A reference names a file or a directory that exists. | holds | relative in-bundle links; ~/.claude/… links; multi-line links with a /-root target split across a newline | **agree** — scan_file records only targets starting `/` or `~/workspace/`, and md.content_lines feeds MD_LINK_PATTERN one line at a time |
| `knowledge-organization.fragment-anchor-matches-the-slug` | A reference that appends `#anchor` to a markdown file names in that anchor the GitHub slug of a heading the file carries. | holds | anchors on relative in-bundle link targets; anchors on ~/.claude/… link targets | **amend** — Same cause as reference-resolves and the same omission: scan_file matches per line, so an anchored link split across two lines is missed too ⇒ anchors on relative in-bundle link targets; anchors on ~/.claude/… link targets; anchors on a link split across two lines |
| `knowledge-organization.link-same-bundle` | A reference to a file or directory in the referencing file's own repository is an inline link whose target is a root-absolute path, beginning `/` and naming the path from the repository root. | holds | relative link to an in-repo file; ~/.claude/… path to an in-repo file; bare-prose reference to an in-repo file | **agree** — wrong-form fires only for a `~/workspace/<repo>/` citation naming the invoking repo from a fixed-root source; no other form is even recorded by scan_file |
| `knowledge-organization.h1` | A `README.md` holds an H1 heading. | holds | H1 presence in non-root READMEs (docs/decisions/README.md, dotfiles/README.md, harness-recipes/README.md, scripts/README.md, standards/README.md, working-docs/software-factory/docs/README.md) | **agree** — repo-lint's check_doc_shapes opens `root/README.md` alone, and the six listed files are exactly the other tracked README.md files |
| `prose.no-first-person` | A harness-loaded agent instruction never speaks in the first person: the words `I`, `me`, and `my` appear nowhere in it, its frontmatter included, except `I` in the abbreviation `I/O` and any of the three inside a double-quoted utterance, an inline code span, or a fenced block. | holds | non-.md agent-instruction files never voice-scanned; md.classify "excluded" paths skipped though the predicate does not exempt them | **agree** — audit() calls scan_voice inside `if rel.endswith(".md")`, so skills/*/agents/openai.yaml is never voiced, and classify "excluded" (PLAN.md, PROGRESS.md, tmp/) is skipped too |
| `prose.the-banned-word` | No tracked file of the repo, except a path `.prose-lint-exempt` lists, contains the word [elided], bare or plural, in any case, alone or in a compound, its frontmatter, code spans, and fenced blocks included. | breaks | "The document does not contain the banned actor noun, bare or plural, in any case, alone or in a compound, its frontmatter, code spans, and fenced blocks included." | **amend** — "The document" drops the code and config files scan_banned deliberately covers, and "the banned actor noun" names no word a reader could apply ⇒ The file does not contain a word the workspace vocabulary bans, `WORKSPACE_VOCABULARY` in `src/dev_playbook/prose_lint.py`, bare or plural, in any case, alone or in a compound, its frontmatter, code spans, and fenced blocks included. |
| `prose.the-repo-vocabulary` | No tracked file under a directory the repo's `.prose-lint-vocabulary` names for a word contains that word, bare or plural, in any case, its frontmatter, code spans, and fenced blocks included; a word declared with no directory is banned in every tracked file of the repo. | holds | a type: Reference document; a md.classify "excluded" path; a symlink; a file carrying a NUL byte | **amend** — The Standard's population already excepts `type: Reference`, so the is_verbatim_doc skip is no gap; the other three skips sit in audit() with no exception behind them ⇒ a md.classify "excluded" path; a symlink; a file carrying a NUL byte |
| `python.docstrings` | Every module, class, function, and method in the file carries a docstring, except a file named `__init__.py` and a pytest test function, whose name begins with `test_`. | breaks | pyproject.toml, scripts/, tests/, src/ | **amend** — The column names paths, not gaps: ruff D skips private and nested defs, per-file-ignores drops D over tests/**, and ruff's include never reaches extensionless scripts ⇒ ruff D covers public, non-nested definitions only, so every `_name` and nested def under src/ passes; `tool.ruff.lint.per-file-ignores` in the canonical pyproject.toml ignores D under `tests/**`, wider than the rule's `test_` exception; ruff's include is `*.py`, so no scripts/ file is linted at all |
| `python.formatted-by-ruff-format` | The file is byte-identical to the output of `ruff format` run under the `line-length` that the canonical pyproject.toml pins. | unknown | identify's tagging of a `#!/usr/bin/env -S uv run --script` shebang, so the pre-commit ruff-format hook never sees it; ruff's default `include` over an extensionless file, so `make format-check` never sees it either | **agree** — The ruff hooks take identify's `python` tag, which a `uv run --script` shebang does not earn, and `ruff format --check .` includes `*.py` only |
| `python.annotated-signatures` | Every function and method in the file annotates each parameter, except a `self` or `cls` first parameter, and its return. | breaks | Makefile, standards/build/canonical/Makefile, scripts/griffe-outline, scripts/harness-files-lint | **amend** — No `standards/build/canonical/Makefile` exists — the fragment is Makefile.python — and the gap is the mypy roots: Makefile line 10 runs `mypy src tests` ⇒ mypy runs over `src tests` only (Makefile:10; repo-lint's check_makefile gives a root its slot only where it holds a `.py` file, and scripts/ holds none), so no extensionless scripts/ file is typechecked — griffe-outline (6 defs) and harness-files-lint's main() carry unannotated signatures today |
| `testing.access-only-public-names` | A test reaches a module that is not itself a test module only through identifiers that do not begin with an underscore: not an imported name, not a segment of an imported module path, and not an attribute reached through an imported name. | holds | never reads tests/conftest.py, tests/transcript_fakes.py, tests/cloa_viewer_fixtures.py; misses a locally rebound private name; misses a relative `from . import _x` | **agree** — scan_file returns early unless the name begins `test_`; visit_Assign maps a rebound name to "", and `from . import _x` leaves node.module None, so the private-name loop is skipped |
| `tracking.closed-fences` | A leaf that carries `mode:session`, or a leaf that carries a `phase:*` label other than `phase:intake`, closes every code fence its body opens. | holds | leaves whose `mode:*` labels do not number exactly one; leaves whose single `mode:*` value is not `direct`, `spike`, or `session` | **agree** — _brief_findings returns [] when `len(modes) != 1` and for any mode outside direct/spike/session, so _heading_findings, which owns the UnclosedFence catch, never runs |
| `tracking.wayfinder-body` | A map's body carries a `Destination`, a `Notes`, a `Decisions so far`, a `Not yet specified`, and an `Out of scope` section, and a decision ticket's body carries a `Question` section, each as a markdown heading at any level. | breaks | Rewrite the Standard's population to an open issue, as under `tracking.build-labels`. | **amend** — Narrowing the population to an open issue is right — gh api asks `state=open` at workspace_lint.py:590 — but the row names no detector gap, and _has_section reads the raw body ⇒ population → "an open GitHub issue in a governed repo"; gaps: _has_section scans the raw body, so a `##` section quoted inside a fence forges one (tracked GeoffNordling/dev-playbook#386), and a closed issue is never read |

## Out of scope: breaks in other repos (14)

| id | rule | evidence | gist |
|---|---|---|---|
| `distribution.the-roster` | A governed repo is named in workspace-lint's `GOVERNED` roster, and every name in the roster is a repo under the workspace root. | other-repo | lunch and date-tree named in `GOVERNED` roster (`src/dev_playbook/workspace_lint.py:160-171`) have no local clone under ~/workspace, though both exist on GitHub. |
| `tracking.written-for-the-user` | An issue's body is readable unaided by a user who sees only the issue and never the author's context, and a reference to existing file content quotes verbatim the text it amends. | github | #444's Desired behavior cites other issues only by reference; a reader alone learns nothing of the work's shape. |
| `tracking.behavioural-not-procedural` | The body of an issue that carries `mode:direct` or `mode:session` describes what the system does after the work, as interfaces and behavioural contracts, never the steps that get there. | github | #400's Artifacts section edits `/software-factory/user-checkpoints.md` with literal before/after steps; #452 also lists steps, both banned by the predicate. |
| `tracking.one-goal` | An issue that carries `mode:direct` or `mode:session` serves one outcome, and every part that could slip indefinitely with the outcome still standing, and could ship later as its own issue without reopening this one, is named under the issue's `Out of scope` heading and carried by a separate issue that carries `phase:intake`. | github | #452's Out of scope names work 'not worth tracking,' carried by no issue, and compares to a second member. |
| `tracking.user-intent` | Where an issue's body carries a `User intent` heading, what stands under it is the user's own words for this issue, never an agent's paraphrase. | github | #443 and #444 both read 'NA - to be discussed with user during /design' — not the user's own words. |
| `tracking.build-labels` | A build leaf that carries a `phase:*` label other than `phase:intake` carries exactly one label from each of `category`, `mode`, `tests`, and `phase`, and each of those labels is a value of the label scheme. | github | 25 closed build leaves (#44, #59, #84, #133, #144-155, #163-200) lack `category:*`; `workspace_lint.py:584` already filters to open issues only. |
| `tracking.build-headings` | A build leaf that carries a `phase:*` label other than `phase:intake` carries every heading below in its body, each as a bold heading; a heading shown inside a code fence is quoted, not carried. | github | 61 of 66 closed post-intake leaves miss a heading; five recent ones (e.g. #452) miss only `Prohibited surfaces`, which none needed. |
| `tracking.session-headings` | A session leaf carries `Summary`, `User intent`, `Current behavior`, `Desired behavior`, `Acceptance criteria`, and `Out of scope` in its body, each as a bold heading; a heading shown inside a code fence is quoted, not carried. | github | #478 writes its six sections as `##` markdown headings, not bold text, so `_has_heading` reports six false findings. |
| `tracking.category-only` | An epic carries exactly one `category:*` label, a value of the label scheme, and carries no `phase:*`, `mode:*`, or `tests:*` label. | github | #142, a closed epic with no labels at all, breaks the 'exactly one category label' clause; nine others comply. |
| `tracking.epic-headings` | An epic carries `Outcome` and `Decomposition rationale` in its body, each as a bold heading. | github | #341 uses markdown headings, not bold text; #293 has neither — `workspace_lint.py:1089` already parses this exact shape. |
| `tracking.no-child-list` | An epic's body does not list its sub-issues. | github | #293's `## Chunk index` table lists six of nine sub-issues by number, duplicating the tracker's own status column. |
| `tracking.wayfinder-body` | A map's body carries a `Destination`, a `Notes`, a `Decisions so far`, a `Not yet specified`, and an `Out of scope` section, and a decision ticket's body carries a `Question` section, each as a markdown heading at any level. | github | #374, a wayfinder:research harvest ticket, records a conversation instead of asking a question, so lacks `Question`; see `workspace_lint.py:784`. |
| `tracking.structure` | Every entry sits under a `##` heading, directly or nested under a parent entry; a heading groups its entries and carries no other meaning, and a nested entry is work that achieves its parent's outcome. | other-repo | sounds/CANDIDATES.md has no `##` heading; its one entry sits directly under the `# Candidates` title |
| `tracking.valid-labels` | A governed repo's GitHub labels are exactly the labels declared in the scheme data `src/dev_playbook/label_scheme.json`, each carrying the color and the description that data gives it. | github | lunch is missing `mode:session` and carries placeholder descriptions for its 17 labels, unlike src/dev_playbook/label_scheme.json |

## Acronyms

- **ADR** — Architecture Decision Record.
- **AST** — Abstract Syntax Tree.
- **H1** — markdown heading level one.
- **H2** — markdown heading level two.
- **H3** — markdown heading level three.
- **JS** — JavaScript.
- **NUL** — the zero byte.
- **OKF** — Open Knowledge Format.
- **PR** — pull request.
- **SHA** — the hash naming one commit.
- **URL** — Uniform Resource Locator.
