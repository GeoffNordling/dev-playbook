---
type: General-Sheet
title: Detector Fixes
description: Step 12's input — the fourteen rules whose detector tests less than the sentence, the survey of what each detector misses, the thin-shim moves, and the one-base check, none of them touched by step 11
---

# Detector Fixes

What step 11 leaves for step 12 to close. Step 11 settles the sentences;
a detector that tests less than its sentence stays as it is, and the gap
is recorded here rather than fixed. This sheet is the whole input: the
rule ids, the survey behind them, and the two moves that ride along.

## The fourteen weak checks

The rules whose detector tests less than the sentence:

`build.every-canonical-file-has-a-rule-and-every-rule-a-file`, `doc-type.entries-point-and-condition`, `doc-type.references-one-level-deep`, `doc-type.three-verb-sections`, `knowledge-organization.fragment-anchor-matches-the-slug`, `knowledge-organization.readme-holds-an-h1`, `knowledge-organization.root-absolute-path-in-the-same-repo`, `knowledge-organization.alphabetical-unless-declared-otherwise`, `knowledge-organization.one-entry-per-concept-document-and-child-directory`, `prose.no-first-person`, `prose.no-word-the-repo-bans`, `standard.the-boundary-table-generated-from-the-wiring`, `tracking.closed-fences`, `tracking.map-sections-ticket-question`.

## What each detector misses

The survey the rule audit produced, the evidence behind the list above.
A row's `not checked` column is the gap; a row whose verdict reads
`disagree — full` records a detector that turned out to cover its
sentence after all, and is kept so step 12 does not re-open it.

| id | rule | state | not checked | verdict |
|---|---|---|---|---|
| `standard.the-boundary-table-generated-from-the-wiring` | A repo that asks any check, dev-playbook or a consumer whose `.pre-commit-config.yaml` wires a `scripts/` hook, carries `standards/boundaries.yaml`, byte-identical to what `scripts/boundary-table` writes: one row per address the verifier table names, sorted, each carrying the gates that run it in a fixed order drawn from `commit`, the pre-commit stage at `git commit`; `push`, the pre-push stage at `git push`, which runs `make check`; `ci`, a workflow under `.github/workflows/`; and `on-demand`, no gate. | holds | that boundaries.yaml's addresses equal verifiers.yaml's (rebuilt independently, not read back); a detector whose claimed ids are all rejected still gets a boundaries.yaml row without one in verifiers.yaml | **agree** — boundary_table.addresses() rebuilds from DETECTORS, UNGATED_AUDITS and DEPENDENCY_RULES and never reads verifiers.yaml, so a detector whose ids verifier_table rejects still gets a row |
| `standard.every-detector-is-reachable-and-listed` | A first-party detector is published as a hook in its repo's `.pre-commit-hooks.yaml` and has a row in the validation table of `scripts/README.md` where the repo has that file. | breaks | A first-party detector is reachable from its repo's published hook — named in the `playbook-lint` roster, or published as its own hook in `.pre-commit-hooks.yaml` — and has a row in the validation table of `scripts/README.md` where the repo has that file. | **amend** — workspace-lint is a first-party detector verifiers.yaml names, yet sits in no roster and no manifest, and its scripts/README.md row is in the Utility table ⇒ A first-party detector is reachable from its repo's published hook — named in the `playbook-lint` roster, wired as a `scripts/` hook its `.pre-commit-config.yaml` and `.pre-commit-hooks.yaml` both carry, or registered as an ungated audit — and has a row in a `scripts/README.md` script table where the repo has that file. |
| `standard.offered-by-the-canonical-template` | A first-party detector in the repo that carries `standards/build/canonical/` is a hook of that canonical `.pre-commit-config.yaml`'s pinned block. | breaks | In the repo that carries `standards/build/canonical/`, the canonical `.pre-commit-config.yaml`'s pinned dev-playbook block offers exactly the hook ids `.pre-commit-hooks.yaml` publishes. | **amend** — The sentence matches check_hook_surfaces' canonical leg, but this is an H3 under `standard.a-first-party-detector`, so its member must be a detector, not the template file ⇒ A first-party detector the repo carrying `standards/build/canonical/` publishes in `.pre-commit-hooks.yaml` is a hook of that canonical `.pre-commit-config.yaml`'s pinned dev-playbook block, which offers exactly the ids that manifest publishes. |
| `standard.every-subdirectory-a-standard-directory` | Every immediate subdirectory of `standards/` is a Standard directory: it holds at least one file typed `Standard`, and every other `.md` file under it, `index.md` aside, is typed `Standard` or `Explanation`; the only flat files under `standards/` are `README.md` and `index.md`; standards-lint reports a departure. | breaks | …; the only flat files under `standards/` are `README.md`, `index.md`, `verifiers.yaml`, and `boundaries.yaml`; standards-lint reports a departure. | **amend** — standards_lint._flat_strays reads flat `.md` only, so naming verifiers.yaml and boundaries.yaml leaves every other flat file undecided; scoping the clause to `.md` closes it ⇒ Every immediate subdirectory of `standards/` is a Standard directory: it holds at least one file typed `Standard`, and every other `.md` file under it, `index.md` aside, is typed `Standard` or `Explanation`; the only flat `.md` files under `standards/` are `README.md` and `index.md`; standards-lint reports a departure. |
| `build.pre-commit-configyaml-holds-every-canonical-block` | `.pre-commit-config.yaml` holds every block of the canonical .pre-commit-config.yaml verbatim and in order; hooks may follow inside a block, further `repo:` blocks may sit between blocks, and the line pinning the dev-playbook `rev` carries the consumer's own value. | holds | `default_install_hook_types: [pre-commit, pre-push]`; `repos:` | **disagree** — full — config_segments splits at "  - repo:", so the preamble `default_install_hook_types` and `repos:` is segment 0 and find_run checks it; only the rev block is skipped, which the rule exempts |
| `build.every-canonical-file-has-a-rule-and-every-rule-a-file` | Every file directly under `standards/build/canonical/` is one a rule of this Standard names, and every file a rule of this Standard names is directly under `standards/build/canonical/`. | holds | manifest doesn't learn of a rule added to canonical.md; manifest still demands a file whose rule is deleted from canonical.md | **agree** — CANONICAL_MANIFEST is a hard-coded frozenset in scripts/repo-lint; check_canonical_artifacts diffs the directory against that set, never against the rules of canonical.md |
| `build.the-repo-directory-names-the-project-and-package` | The root `pyproject.toml` sets `project.name` to the repo directory's name lowercased, `My-Repo` → `my-repo`, and the import package is that name with each hyphen an underscore, `my_repo`. | breaks | The root `pyproject.toml` sets `project.name` to the repository's own name lowercased, the directory holding the shared `.git` and therefore the same from the main checkout and from every worktree, `My-Repo` → `my-repo`, and the import package is that name with each hyphen an underscore, `my_repo`. | **agree** — gitrepo.canonical_repo_name returns the parent of `git rev-parse --git-common-dir`, so a worktree answers `dev-playbook`, not its own directory name; the rewrite states that |
| `doc-type.three-verb-sections` | After the graph, a document typed `Loop` holds three H2s, `Acts`, `Checks`, and `Yields`, in that order and no others, with nothing between the graph and the first of them. | holds | "a blank line between two separate markdown lists under the same H2 is invisible to it, and both lists read as one run of entries" | **agree** — entries_of skips blank lines (`if not line.strip(): continue`), so two lists under one H2 read as one run of entries; every other clause is checked |
| `doc-type.entries-point-and-condition` | Every entry of a document typed `Loop` states its condition, `fires when …` or `fires every iteration` for an act or a check and `yields when …` for a yield. | holds | "A second or third link in an act's or a check's entry — a pointer at a guide beside the runbook, say — is never resolved, so a dead link there passes" | **agree** — check_entry resolves only links[0] for an act or a check (a yield resolves every link), so a second link's target is never opened |
| `doc-type.references-one-level-deep` | No `.md` file in a skill's `references/` links to another `.md` file in that `references/`. | holds | skips links starting with ~ or /; only scans references/*.md, not subdirectories; LINK_RE matches inline links only, missing reference-style definitions | **agree** — check_references_depth skips targets starting `~` or `/`, globs `references/*.md` only, and LINK_RE matches inline `[t](x)` alone |
| `knowledge-organization.type-names-a-registered-type` | A concept document's frontmatter has a `type` key whose value names one row of the table below, or one entry of the `okf_types` mapping in the frontmatter of the repo's own root `index.md` ([Type Registry](/standards/knowledge-organization/type-registry.md#local-declaration)). | holds | - | **disagree** — full — check_types flags a missing `type` and a value outside `registry`, which is the `## Types` table in apex mode and upstream plus okf_types names in consumer mode |
| `knowledge-organization.local-declaration` | The declaration is the `okf_types` mapping in the frontmatter of the root `index.md` of a repo that does not carry `standards/build/canonical/`, one entry per document type the repo declares for itself. | holds | Where the mapping lives; The apex exclusion; One entry per type | **agree** — The two LOCAL_DECLARATION sites flag a non-mapping okf_types and a legacy registry document; apex mode never reads okf_types, and no site counts entries per type |
| `knowledge-organization.one-entry-per-concept-document-and-child-directory` | An `index.md` lists, as a bullet holding a root-absolute markdown link and exactly once each, every concept document in its own directory and every child directory's own `index.md`, and lists nothing else; each concept document's entry carries that document's frontmatter `description` verbatim. | holds | exactly once each, for child indexes; lists nothing else, for a non-link item | **agree** — check_indexes Counters duplicates for concepts only and turns listed_children into a set; parse_index matches INDEX_BULLET, so a bullet with no root-absolute link is invisible |
| `knowledge-organization.alphabetical-unless-declared-otherwise` | Within each group of an `index.md`'s listing, the concept documents and then the child-directory links, entries are in alphabetical order by link title, compared case-insensitively, and a `README.md` entry is the first entry of the whole listing; an introduction line beginning `Ordering:`, one before the first listed entry, releases the alphabetical order of both groups and never the `README.md` entry's place. | holds | the first entry of the whole listing; the group order itself | **agree** — check_index_ordering takes the README index within group_a, which holds concept bullets only, and sorts the two groups apart, never one against the other |
| `knowledge-organization.the-root-index` | The `index.md` at the repository root. | holds | - | **disagree** — full — the predicate only names the root `index.md`, and check_indexes emits THE_ROOT_INDEX exactly when `()` is no index directory, which is that file's absence |
| `knowledge-organization.reference-resolves` | A reference names a file or a directory that exists. | holds | relative in-bundle links; ~/.claude/… links; multi-line links with a /-root target split across a newline | **agree** — scan_file records only targets starting `/` or `~/workspace/`, and md.content_lines feeds MD_LINK_PATTERN one line at a time |
| `knowledge-organization.fragment-anchor-matches-the-slug` | A reference that appends `#anchor` to a markdown file names in that anchor the GitHub slug of a heading the file carries. | holds | anchors on relative in-bundle link targets; anchors on ~/.claude/… link targets | **amend** — Same cause as reference-resolves and the same omission: scan_file matches per line, so an anchored link split across two lines is missed too ⇒ anchors on relative in-bundle link targets; anchors on ~/.claude/… link targets; anchors on a link split across two lines |
| `knowledge-organization.root-absolute-path-in-the-same-repo` | A reference to a file or directory in the referencing file's own repository is an inline link whose target is a root-absolute path, beginning `/` and naming the path from the repository root. | holds | relative link to an in-repo file; ~/.claude/… path to an in-repo file; bare-prose reference to an in-repo file | **agree** — wrong-form fires only for a `~/workspace/<repo>/` citation naming the invoking repo from a fixed-root source; no other form is even recorded by scan_file |
| `knowledge-organization.readme-holds-an-h1` | A `README.md` holds an H1 heading. | holds | H1 presence in non-root READMEs (docs/decisions/README.md, dotfiles/README.md, harness-recipes/README.md, scripts/README.md, standards/README.md, working-docs/software-factory/docs/README.md) | **agree** — repo-lint's check_doc_shapes opens `root/README.md` alone, and the six listed files are exactly the other tracked README.md files |
| `prose.no-first-person` | A harness-loaded agent instruction never speaks in the first person: the words `I`, `me`, and `my` appear nowhere in it, its frontmatter included, except `I` in the abbreviation `I/O` and any of the three inside a double-quoted utterance, an inline code span, or a fenced block. | holds | non-.md agent-instruction files never voice-scanned; md.classify "excluded" paths skipped though the predicate does not exempt them | **agree** — audit() calls scan_voice inside `if rel.endswith(".md")`, so skills/*/agents/openai.yaml is never voiced, and classify "excluded" (PLAN.md, PROGRESS.md, tmp/) is skipped too |
| `prose.no-banned-word` | No tracked file of the repo, except a path `.prose-lint-exempt` lists, contains the word [elided], bare or plural, in any case, alone or in a compound, its frontmatter, code spans, and fenced blocks included. | breaks | "The document does not contain the banned actor noun, bare or plural, in any case, alone or in a compound, its frontmatter, code spans, and fenced blocks included." | **amend** — "The document" drops the code and config files scan_banned deliberately covers, and "the banned actor noun" names no word a reader could apply ⇒ The file does not contain a word the workspace vocabulary bans, `WORKSPACE_VOCABULARY` in `src/dev_playbook/prose_lint.py`, bare or plural, in any case, alone or in a compound, its frontmatter, code spans, and fenced blocks included. |
| `prose.no-word-the-repo-bans` | No tracked file under a directory the repo's `.prose-lint-vocabulary` names for a word contains that word, bare or plural, in any case, its frontmatter, code spans, and fenced blocks included; a word declared with no directory is banned in every tracked file of the repo. | holds | a type: Reference document; a md.classify "excluded" path; a symlink; a file carrying a NUL byte | **amend** — The Standard's population already excepts `type: Reference`, so the is_verbatim_doc skip is no gap; the other three skips sit in audit() with no exception behind them ⇒ a md.classify "excluded" path; a symlink; a file carrying a NUL byte |
| `python.docstrings` | Every module, class, function, and method in the file carries a docstring, except a file named `__init__.py` and a pytest test function, whose name begins with `test_`. | breaks | pyproject.toml, scripts/, tests/, src/ | **amend** — The column names paths, not gaps: ruff D skips private and nested defs, per-file-ignores drops D over tests/**, and ruff's include never reaches extensionless scripts ⇒ ruff D covers public, non-nested definitions only, so every `_name` and nested def under src/ passes; `tool.ruff.lint.per-file-ignores` in the canonical pyproject.toml ignores D under `tests/**`, wider than the rule's `test_` exception; ruff's include is `*.py`, so no scripts/ file is linted at all |
| `python.formatted-by-ruff-format` | The file is byte-identical to the output of `ruff format` run under the `line-length` that the canonical pyproject.toml pins. | unknown | identify's tagging of a `#!/usr/bin/env -S uv run --script` shebang, so the pre-commit ruff-format hook never sees it; ruff's default `include` over an extensionless file, so `make format-check` never sees it either | **agree** — The ruff hooks take identify's `python` tag, which a `uv run --script` shebang does not earn, and `ruff format --check .` includes `*.py` only |
| `python.annotated-signatures` | Every function and method in the file annotates each parameter, except a `self` or `cls` first parameter, and its return. | breaks | Makefile, standards/build/canonical/Makefile, scripts/griffe-outline, scripts/harness-files-lint | **amend** — No `standards/build/canonical/Makefile` exists — the fragment is Makefile.python — and the gap is the mypy roots: Makefile line 10 runs `mypy src tests` ⇒ mypy runs over `src tests` only (Makefile:10; repo-lint's check_makefile gives a root its slot only where it holds a `.py` file, and scripts/ holds none), so no extensionless scripts/ file is typechecked — griffe-outline (6 defs) and harness-files-lint's main() carry unannotated signatures today |
| `testing.access-only-public-names` | A test reaches a module that is not itself a test module only through identifiers that do not begin with an underscore: not an imported name, not a segment of an imported module path, and not an attribute reached through an imported name. | holds | never reads tests/conftest.py, tests/transcript_fakes.py, tests/cloa_viewer_fixtures.py; misses a locally rebound private name; misses a relative `from . import _x` | **agree** — scan_file returns early unless the name begins `test_`; visit_Assign maps a rebound name to "", and `from . import _x` leaves node.module None, so the private-name loop is skipped |
| `tracking.closed-fences` | A leaf that carries `mode:session`, or a leaf that carries a `phase:*` label other than `phase:intake`, closes every code fence its body opens. | holds | leaves whose `mode:*` labels do not number exactly one; leaves whose single `mode:*` value is not `direct`, `spike`, or `session` | **agree** — _brief_findings returns [] when `len(modes) != 1` and for any mode outside direct/spike/session, so _heading_findings, which owns the UnclosedFence catch, never runs |
| `tracking.map-sections-ticket-question` | A map's body carries a `Destination`, a `Notes`, a `Decisions so far`, a `Not yet specified`, and an `Out of scope` section, and a decision ticket's body carries a `Question` section, each as a markdown heading at any level. | breaks | Rewrite the Standard's population to an open issue, as under `tracking.one-label-from-each-prefix`. | **amend** — Narrowing the population to an open issue is right — gh api asks `state=open` at workspace_lint.py:590 — but the row names no detector gap, and _has_section reads the raw body ⇒ population → "an open GitHub issue in a governed repo"; gaps: _has_section scans the raw body, so a `##` section quoted inside a fence forges one (tracked GeoffNordling/dev-playbook#386), and a closed issue is never read |

## The two moves that ride along

The five thin shims that carry rule logic move it into the package
(`standard.the-script-holds-no-rule-logic`), and `doc-type.one-base-class`
gains its check.

## Acronyms

None.
