# Flag

Run `uv run --directory /Volumes/workplace/dev-tools flag "<message>"` with the user's message and return to work. No follow-up questions. If `/flag` was invoked with no arguments, ask for a one-sentence description.
