# dotfiles/ — index

The agent's own configuration held as version-controlled source rather than
edited in `$HOME` — stow installs it from here, so this tree is where every
skill, rule, and setting is changed. Start at
[Dotfiles](/dotfiles/README.md).

- [Dotfiles](/dotfiles/README.md) — Claude Code configuration — skills, rules, settings, hooks — managed via GNU Stow, symlinked into home
