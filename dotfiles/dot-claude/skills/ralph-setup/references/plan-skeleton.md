# Plan: <one-line goal>

<!-- AUTHORING (ralph-setup): replace the <angle-bracket> placeholders with the
approved content, then delete these authoring comments. Keep the section
structure and the orientation paragraph below — the running loop reads them. -->

A Ralph loop works this file top to bottom. Each iteration, a fresh agent with
no memory reads this plan and the progress log, does the first unchecked task,
checks it off, and commits. Everything it needs to act is here — including the
Working notes below, where earlier iterations leave facts you will need. Add to
them whenever you learn something a future iteration would otherwise rediscover.

## Done when

<!-- The finished product as checkable criteria. The loop is complete only when
every task below is checked and these all hold. -->

- <criterion>
- <criterion>

## When the sources do not settle it

You will hit points this plan and its sources do not answer. Do not stop for
them, and do not decide them silently. Take the smallest step that keeps the
check gate green, record it as a judgment call in the progress log, and carry
on. A reviewer reads every one of them at the next checkpoint and rules.

1. The sources win, even where you can see a better idea. Record the better
   idea as a judgment call instead of acting on it.
2. Stay inside your task. Never edit the sources, reorder the plan, or work a
   task that is not yours.
3. Stop only for something whole and missing that no source names — then set a
   blocker rather than inventing it.

## Working notes

<!-- Durable facts a future memoryless iteration needs: where things live,
gotchas, conventions, settled decisions. The progress log is the chronological
one-line-per-iteration record; this is the curated, current knowledge base.
Seed any facts known from setup; the loop keeps it tight and current as it
learns. A checkpoint reviewer writes its accepted rulings here as plain facts. -->

- <fact a later iteration would otherwise have to rediscover, or empty at setup>

## Tasks

<!-- Sequential. Each task is small enough for one iteration, independently
committable, leaves the check gate green, and depends only on tasks above it. The
agent does the first unchecked task only. The order is fixed once the loop
runs.

Every task carries a Verify clause: what proves it landed, checked against the
artifact rather than against the agent's word for it. A checkpoint reviewer runs
these, so name a command where a command settles it.

`<!-- [ ] checkpoint -->` lines cut the list into segments — as many as were
agreed at setup, and always one after the last task. Copy the marker character
for character; the loop refuses to start on one it cannot parse. -->

- [ ] <task — specific enough to execute without re-deriving the plan>
      Verify: <the command, file, or behavior that proves it landed>
- [ ] <task>
      Verify: <...>

<!-- [ ] checkpoint -->

- [ ] <task>
      Verify: <...>

<!-- [ ] checkpoint -->
