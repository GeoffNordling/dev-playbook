# Progress log

The running memory of this Ralph loop. Each iteration appends one line below —
what it did and what is next — newest at the bottom. A fresh agent reads this
before starting, to see what the iterations before it already did.

## Log

<!-- iterations append one line each below this line -->
- T1 done: scaffolded `tools/transcript_export/` (`__init__.py`, `cli.py` with `main`→usage), thin shim `tools/bin/transcript-export`, added `transcript_export/` to Makefile mypy target, one unit test; `make -C tools check` green. Next: T2 — AgentsView client.
- T2 done: added `transcript_export/client.py` (`session_list`/`session_get`/`session_messages` over an injectable `runner`, `AgentsViewError` fail-loud, page-100 paging on last_ordinal+1) + 6 unit tests in `tests/test_transcript_export_client.py`; `make -C tools check` green (187 passed). Next: T3 — message model + resume dedup.
- T3 done: added `transcript_export/model.py` — frozen `Message`/`ToolCall` dataclasses, `message_from_row`/`tool_call_from_row`, `collapse_resume_duplicates` (rule 1, keep-first by source_uuid), `normalize_messages` (map rows + dedup, order preserved) + 7 unit tests in `tests/test_transcript_export_model.py`; `make -C tools check` green (194 passed). Next: T4 — fork reconstruction (tree from source_parent_uuid, live path, abandoned branches).
- T4 done: added `transcript_export/forks.py` — `reconstruct_forks` returns `ForkReconstruction(live_path, abandoned_branches keyed by fork point)` built from the `source_parent_uuid`→`source_uuid` tree, live path = root→tip ancestor chain of the highest-ordinal message, abandoned heads grouped by parent (root-level forks key on the absent/None parent), `MessageNode` preserves nested forks; 9 unit tests in `tests/test_transcript_export_forks.py`; `make -C tools check` green (203 passed). Next: T5 — record classification + dedup rule 2.
- T5 done: added `transcript_export/classify.py` — `MessageKind` enum, `classify` (keys off source_type/source_subtype, compaction-boundary checked before system->drop, unexpected source_type raises), `collapse_adjacent_repeats` (dedup rule 2), `keep_messages` (drop plumbing then rule 2); 15 unit tests in `tests/test_transcript_export_classify.py`; `make -C tools check` green (218 passed). Next: T6 — render header + turns + thinking.
- T6 done: added `transcript_export/render.py` — `escape` (all five entities, `&` first; reused for text + attrs), `render_session_open` (maps `session get` payload → `<session>` attrs, `id` required/fail-loud, optionals omitted when None, zero counts kept), `render_turn` (classify-dispatched `<user>`/slash/`<assistant>` with `<thinking>`, fails loud on non-turn); live-verified header keys; 17 unit tests (ElementTree round-trip) in `tests/test_transcript_export_render.py`; `make -C tools check` green (235 passed). NOTE: turn-text marker stripping deferred to T7. Next: T7 — render tool calls (+ turn-text marker stripping).
- T7 done: extended `render.py` — `render_tool_call` (`<tool-call name id [outcome]>` + escaped `<args>` from `input_json` + `<output chars=result_content_length truncated>` cut at 2000; outcome rejected/error from output text) and `strip_tool_markers` (cuts the trailing `[ToolName: detail]`/`$ command` block at the Nth marker-from-end, N=len(tool_calls); live-verified 1 marker/call across 554 msgs, handles multiline `$` + parallel calls); `render_turn` now strips markers from prose and embeds one `render_tool_call` per assistant `tool_calls[]` after thinking+prose; 14 new unit tests; `make -C tools check` green (249 passed). Sub-agent `<subagent>` nesting is T9. Next: T8 — render markers (`<compaction>`, `<interrupted/>`) + `<rewound-branch>`.
- T8 done: extended `render.py` — `render_compaction` (`<compaction>summary</compaction>`, no attrs, fail-loud on non-compaction), `render_interrupted` (self-closing `<interrupted ord/>`, fail-loud), `render_message` (single per-message dispatcher: COMPACTION/INTERRUPT→markers, DROP→"", else `render_turn`), and `render_rewound_branch(MessageNode)` (one abandoned head → `<rewound-branch>` in full fidelity via `render_message`; nested fork keeps highest-ordinal child inline, wraps lower-ordinal siblings as nested `<rewound-branch>`); 12 new unit tests; `make -C tools check` green (261 passed). T10 should reuse `render_message` for the live-path walk and emit one `<rewound-branch>` per `abandoned_branches[fork]` head. Next: T9 — sub-agent recursion (`subagent_session_id`→nested `<subagent>` inside `<tool-call>`, cycle+depth guards).
- T9 done: added `transcript_export/transcript.py` — `render_session(id, runner)` (the single per-session entry point: `session_get` header + `_render_body` + `<session>…</session>`) and `_render_body` (shared walk for `<session>`/`<subagent>`: `keep_messages(normalize_messages)`→`reconstruct_forks`→emit off-live-path branches up front, then live path with `render_rewound_branch` at each fork point). Sub-agent expansion threaded as an injected `render_subagent` callback through `render_tool_call`/`render_turn`/`render_message`/`render_rewound_branch` (default None = pure render, earlier tests untouched); `_subagent_renderer` recurses with cycle (`visited`) + depth (`MAX_SUBAGENT_DEPTH=8`) guards → self-closing `<subagent omitted="cycle|depth"/>`. 10 new unit tests vs a fake daemon; `make -C tools check` green (271 passed). NOTE: the full live-path walk now lives in `transcript.py`; T10 reuses `render_session` unchanged (CLI selection + file write only). Open question logged in PLAN working notes: keep/drop runs before fork reconstruction — verify on a live rich session in T11. Next: T10 — CLI selection (explicit ids / `--recent N` / `--all`) + write `<out_dir>/<id>.xml`.
- T10 done: rewrote `transcript_export/cli.py` — `_parse_args` (positional `out_dir` + `session_ids`, `--recent N`, `--all`; enforces exactly one selection mode + positive N), `select_session_ids` (explicit ids pass through with no daemon call; `--recent`/`--all` slice `session_list`'s newest-first sessions), `main(argv, runner, render)` writes one `<out_dir>/<id>.xml` per session (mkdir parents, idempotent overwrite, injectable `render` for unit tests); rewrote `tests/test_transcript_export.py` with 11 selection/validation/output tests; `make -C tools check` green (282 passed). FOUND (logged in PLAN working notes as a T11 bug): a live `--recent 1` smoke test crashes `KeyError: 'source_uuid'` — a real `source_subtype=queued_command` user row lacks `source_uuid` but `message_from_row` hard-requires it; T11 must handle/drop it. Next: T11 — live-daemon integration tests (and fix the source_uuid gap).
- T11 done: added live-daemon integration tests (`tests/test_transcript_export_live.py`, skip-guarded; exercises list/get/messages + render_session + CLI file write, asserts well-formed XML) and fixed two real-data crashes — `source_uuid` is now optional (`queued_command` previews lack it: `model.message_from_row` uses `.get`, `collapse_resume_duplicates` skips uuid-less rows, `classify` drops `queued_command` as plumbing) and `render.escape` strips XML-1.0-illegal control bytes (ANSI ESC etc.) so real tool output parses; `forks._uuid` now fails loud if a uuid-less message reaches reconstruction. CRITICAL FINDING (deferred to T12): fork reconstruction is broken on real data — `source_parent_uuid` points at unsurfaced raw sub-records, so the parent→uuid tree never connects (0/62, 2/196 resolve), the live path collapses to 1 message, and a fork-free session renders as ~all `<rewound-branch>` (25 wrappers, 1 real turn); the surviving fork signal is a *shared* parent uuid. Encoded as a strict xfail (`test_forkfree_stream_with_nonresolving_parents_is_all_live`); redesign = ordinal spine + shared-parent fork detection. Renumbered prototype retirement to T13. `make -C tools check` green (294 passed, 1 xfailed). Next: T12 — fix fork reconstruction (ordinal-spine redesign).
- T12 done: rewrote `transcript_export/forks.py` as an ordinal-spine reconstruction — sort by ordinal (the live spine), detect forks by *shared* `source_parent_uuid` (≥2 msgs), highest-ordinal sibling stays live, each lower sibling heads an abandoned branch owning `[sibling_ord, next_sibling_ord)`; helpers `_abandoned_ranges`/`_outermost`/`_segment`/`_build_branch`/`_chain` carve outermost ranges and recurse for nested forks. `abandoned_branches` is now keyed by the **live sibling's source_uuid** (typed `dict[str, …]`), and `_render_body` emits each fork's `<rewound-branch>`es **before** its live message; MessageNode + renderers unchanged. Updated 6 forks tests to the new keying, flipped the strict xfail to passing, added a realistic non-resolving-parents-plus-one-shared-fork fixture. Verified on real d45168f9: full live path (37 user + 231 assistant turns) + exactly the 2 documented abandoned branches (was: ~all rewound). `make -C tools check` green (296 passed). Next: T13 — retire the prototype dir.
- T13 done: `git rm -r tools/transcript-export-prototype/` (README.md + render_transcript.py — the throwaway plain-text/`export`-based reference). Confirmed no other refs exist: the only mentions of the prototype path live in PLAN.md (Done-when criterion, the directional-reference working note, and the task line); no tools/README.md or root README pointed at it. `make -C tools check` green (296 passed). All plan tasks complete.
