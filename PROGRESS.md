# Progress log

The running memory of this Ralph loop. Each iteration appends one line below —
what it did and what is next — newest at the bottom. A fresh agent reads this
before starting, to see what earlier iterations did.

## Log

<!-- iterations append one line each below this line -->

- Task 1 done: `cloa_viewer` and `cloa_viewer.kinds` packages with empty inits, `cli.py` with `parse_args`/`main`, six tests in `tests/dev_playbook/cloa_viewer/test_cli.py`, and the `viewer` group, `dev` additions, `[tool.uv] default-groups`, and `[project.scripts]` in `pyproject.toml`; `uv sync` also rewrote the tracked `uv.lock`, which the "What you may write" list does not name but `uv sync` cannot avoid — flagged for the user, not treated as a stop; `uv run cloa-viewer --help` prints usage and the gate is green. Next: task 2, the state module and the envelope schema.
- Task 2 done: `state.py` with `state_root`/`checkout_dir`/`head_commit`/`branch_name`/`write_checkout_json`/`envelope`/`load_schema`/`validate`/`write_json`/`ContractError`, `schemas/envelope.schema.json` (draft 2020-12, `additionalProperties: false`, all seven fields required), and 13 tests in `tests/dev_playbook/cloa_viewer/test_state.py`; no design disagreement found — the contract member's envelope table and the plan's schema match field for field. Next: task 3, the registry and the `index-tree` kind, which also creates the shared `checkout` fixture.
- Task 3 done: `registry.py` (`KINDS`, `kind_by_name`), `entry.py` (the `Kind` and `View` records), `identity.py` (`resolve_target`, `is_external`), `kinds/index_tree.py`, `schemas/index-tree.schema.json`, and 17 tests across `tests/dev_playbook/cloa_viewer/kinds/test_index_tree.py` and `test_registry.py`; three plan deviations, all forced and all recorded in Working notes — (1) the nested `tests/dev_playbook/cloa_viewer/conftest.py` the task asks for shadows the root `tests/conftest.py` on `sys.path` and broke seven existing suites at collection, so the shared fixture is `build_checkout(tmp_path)` in `tests/cloa_viewer_fixtures.py`, the only place both pytest and mypy resolve a bare import from; (2) `Kind` and `View` live in a new `entry.py` and `registry` re-exports them, because a kind module importing the registry that imports it is a circular import; (3) target resolution went into a new `identity.py` so task 4's `links_out` reuses it instead of copying it. Two smaller readings the plan left open: a listing entry is the *first* link on a `- ` line (a description may carry a citation of its own), and an external listing target is skipped rather than made a file node. Gate green, 1227 passed. Next: task 4, the `markdown-file` kind.
- Task 4 done: `kinds/markdown_file.py` (frontmatter facts, `words`, `headings`, `links_out`, `links_in`, `source`), `schemas/markdown-file.schema.json`, the kind added to `KINDS` after `index-tree`, and 13 tests in `tests/dev_playbook/cloa_viewer/kinds/test_markdown_file.py` plus a third `test_registry.py` case for the per-subject kind; one plan deviation and one reading, both in Working notes — (1) the plan's expected `links_in` for `docs/beta.md` is `["alpha.md"]`, but `docs/index.md` also links to `/docs/beta.md` in the fixture, so the test asserts `["alpha.md", "docs/index.md"]`; (2) a bare `#anchor` target names the file it sits in, or the 15 files here that link to their own headings would all read `broken`. Smoke run on this checkout: 202 views, all validating, 711 `ok` / 20 `external` / 195 `broken`, the broken ones almost all `~/workspace/...` Citations — a design question for the user, not a code fix. Gate green, 1241 passed. Next: task 5, refresh.
- Task 5 done: `refresh.py` (`refresh(checkout)`, plus `_stage`, `_publish`, `_clear`, `_now`), `schemas/refresh.schema.json`, and 11 tests in `tests/dev_playbook/cloa_viewer/test_refresh.py` covering both kinds' files, the record's counts and commit, `checkout.json`, no `.staging` at rest, a raising generator, a generator whose view fails its payload schema, and a failed kind leaving its last good files untouched; one addition to the plan's steps, noted in Working notes — `refresh` validates the record against the refresh schema before writing it, so the writer and the schema the page will read cannot drift; every view is validated before the first write, so one bad view publishes none of them. Smoke run on this checkout: 1 `index-tree` and 202 `markdown-file` views, both `ok`, and the checkout directory holds exactly `checkout.json`, `index-tree.json`, `markdown-file/`, `refresh.json`. Gate green, 1252 passed. Next: task 6, the server routes and the event stream.
- Task 6 done: `server.py` (every route the task names, the SSE stream over a per-page `asyncio.Queue`, `subscribe`/`unsubscribe`/`publish` at module level, and the `watch_state` startup task on a `lifespan`), and 23 tests in `tests/dev_playbook/cloa_viewer/test_server.py`; a live smoke run against this checkout served all nine routes correctly and refused `../../etc/passwd` with a 404. One deviation and one fix, both in Working notes, both forced by a measurement rather than a preference: (1) the plan's per-file watcher rule cannot see a per-subject republish at all — renaming `markdown-file/` into place reports one event on the directory and none on the 202 files inside — so `messages_for` fans a directory event out into one `changed` per view file, and treats a `deleted` whose path exists again as `changed`, since the kernel reports a delete inside a just-renamed directory under the directory's old name; (2) `refresh._publish` now renames the live tree to `<staging>/<kind>/retired` instead of `shutil.rmtree`-ing it in place, because the in-place delete reached the stream as 202 `removed` messages and would have closed every open panel on every refresh. Task 5's code changed for (2); it is inside the writable list and its own tests still pass. Also added: a 200ms watcher debounce (watchfiles defaults to 1600ms, which alone would eat the two-second budget) and per-batch dedupe (499 messages for 203 files before it). Measured after the fix: one refresh publishes exactly 203 `changed`, 1 `refreshed`, 0 `removed`. Gate green, 1275 passed. Next: task 7, the checkout watcher — note that its "edit a file, panel updates" test now has a working event path to rely on.
- Task 7 done: `watch.py` (`watch_checkout`, `outside_git`, `CHECKOUT_DEBOUNCE_MS`), the wiring in `server._lifespan` (one `watch_checkout` task per checkout beside `watch_state`, all cancelled on shutdown), and 3 tests in `tests/dev_playbook/cloa_viewer/test_watch.py` — an edit under a running watcher raises `alpha.md`'s `words` by exactly the four words appended, and the `.git` filter is asserted both ways. No deviation from the plan; two facts added to Working notes. The filter is load-bearing rather than tidy: a refresh runs `git ls-files` and `git rev-parse`, git writes inside `.git` when it does, and without the filter every refresh would retrigger the watcher that started it. Smoke run through the app's own `lifespan_context` on a two-file checkout: one appended line published 3 `changed` and 1 `refreshed` within 0.17s of the write — the whole disk-to-stream loop, against the two-second budget — and nothing more arrived in the three seconds after. Gate green, 1278 passed. Next: task 8, the command, which is the last Python task before the web project.
- Task 8 done: `cli.py` grew `dist_dir`, `current_checkout`, `serve`, and a real `main` (resolve the checkouts, refuse an unbuilt page with status 2, refresh each checkout, name every failed generator on stderr, print the address, then serve), and `test_cli.py` grew to 16 tests. No deviation from the plan; two decisions it left open, both in Working notes — the built page is behind `dist_dir()` rather than a module constant so a test can point the command elsewhere, and `current_checkout` lets `git rev-parse --show-toplevel` raise when the directory is in no repository, since there is then nothing to show. A fake `dist` in a test needs `assets/` as well as `index.html`, because `build_app` mounts `StaticFiles` on it. Smoke run on this checkout through the real `uvicorn`: `uv run cloa-viewer` with no argument found this worktree, refreshed 203 views (`index-tree` 1, `markdown-file` 202, both `ok`), printed `cloa-viewer at http://127.0.0.1:8791/`, and served `/`, `/api/checkouts`, `/api/checkouts/<dir>/files`, and the refresh record; with no `web/dist` yet it prints `page not built: run make web` and exits 2. Gate green, 1288 passed. Next: task 9, the web project, the `web` and `web-check` Makefile targets, and the type-check pre-commit hook — the first task that touches `.gitignore`, `Makefile`, and `.pre-commit-config.yaml` (append only, never an existing line).
- Task 9 done: the web project at `src/dev_playbook/cloa_viewer/web/` (`package.json`, `package-lock.json`, `tsconfig.json`, `vite.config.ts`, `index.html`, `src/main.tsx`, `src/App.tsx`), the `web` and `web-check` Makefile targets, the two `.gitignore` entries, and the `web-typecheck` pre-commit hook; npm resolved React 19.2, ajv 8.20, markdown-it 15.0, TypeScript 7.0, Vite 8.2. One deviation, in Working notes: the task says add `web` and `web-check` to `.PHONY`, but `scripts/repo-lint` compares the canonical `Makefile.python` fragment as one contiguous verbatim run whose first line is that `.PHONY` line, so the targets got their own `.PHONY: web web-check` line below the canonical block — same meaning to Make, and `./scripts/repo-lint .` reports clean. Also noted: `make check` now needs `node_modules`, since the new hook runs `tsc`. Smoke run: `make web` built `dist/index.html` and `dist/assets/index-C6eQrkG5.js`, which is exactly what `build_app` serves; `uv run cloa-viewer --port 8765` printed `cloa-viewer at http://127.0.0.1:8765/`, `curl /` returned the built page and `curl /assets/index-C6eQrkG5.js` returned 200, and the port refused a connection after the stop. Gate green, 1288 passed, the new `web typecheck` hook passing with it. Heads-up for the next iteration: while this one ran, the user settled the citation-link question and committed it as `b84f307` — `PLAN.md` gained a new task 12 (citation links and link identities) and the Playwright task moved to 13, and `worktree-cloa-viewer-tool-working-docs/registry.md` gained a matching note. Re-read the task list before assuming a number. Next: task 10, the page shell.
- Task 10 done: the page shell — `src/api.ts` (one typed function per route plus the `EventSource` wrapper), `src/validate.ts` (Ajv 2020, schemas compiled once by `loadSchemas` so `validateView` is synchronous), `src/store.ts` (`useViewer`, the whole state and the only fetcher), `src/components/{ErrorPanel,Banner,TopBar,PanelStack}.tsx`, `src/kinds/index.ts` (the `RENDERERS` map, empty until task 11), `src/App.tsx`, and `src/app.css` (the three-region grid, 320px left column). Three decisions the task left open, all in Working notes: Ajv must be `ajv/dist/2020` because every schema is draft 2020-12 and plain Ajv is draft-07; the connection has a third state, `connecting`, so the red banner does not flash on every load; and the store gained `openError(path, error)`, which the red refresh status uses now and task 11's `no view file for <identity>` uses next. Chromium smoke run against this checkout: the page fetched and validated all 203 view files with zero non-200 and zero console errors, the top bar read `dev-playbook · worktree-cloa-viewer-tool` with `Refreshed 2026-09-08T19:49:00Z at b7795a6`, no banner while connected, and `Disconnected from cloa-viewer` within a second of the server being killed. That smoke run also measured a trap for task 13 and it is in Working notes: `uv run cloa-viewer` as a subprocess makes `uv` the parent, so `Popen.kill()` orphans the server, and `Popen.terminate()` does not return within 20 seconds while a page holds the event stream — task 13 must start `.venv/bin/cloa-viewer` directly. Gate green, 1288 passed. Next: task 11, the tree renderer.
- Task 11 done: `src/kinds/index-tree/IndexTree.tsx` (the payload's types, expansion held as a set of identities with the root's in it at first, a directory row with its twist and title and muted description and right-aligned word count, a file row falling back to its identity when the title is null and dimmed with a `missing` label when `exists` is false, and the `unindexed` list last as a row titled `Unindexed` reading `N files`), the kind registered in `src/kinds/index.ts`, and the tree wired into the left column. Two additions the task left open, both in Working notes: the resolution from a path to a rendered view moved out of `PanelStack` into `src/components/ViewBody.tsx` so the tree region and the panel stack fail identically on a bad view file, and the store gained `loaded`, without which the left column drew a red `no view file` on every load while the fetch was still out. Clicking a file row calls `openPanel` when the store holds `markdown-file/<identity>.json` and `openError` with `no view file for <identity>` when it does not, so the browser resolves no paths. Smoke run against this checkout through Playwright (the Chrome extension was not connected this session): the root drew 14 rows with `dev-playbook — bundle index` first and `index.md` as its own first child, clicking `doc-types/ — index` took the tree from 14 rows to 20, clicking the `Doc-Type` file row opened a panel titled `Doc-Type` reading `no renderer for kind markdown-file` (task 13's job), `Unindexed` read `57 files` and expanded to 77 rows, and the console carried zero errors. Gate green, 1288 passed. Next: task 12, citation links and link identities — Python only, and it changes `tests/cloa_viewer_fixtures.py`, so re-read the task before touching the fixture.
- Task 12 done: `identity.py` gained `WORKSPACE_PREFIX`, `citation_repo`, and a third `repo` parameter on `resolve_target`; `kinds/markdown_file.py` gained the `Link` TypedDict, `_link` (external, then citation, then ok/broken by identity), and `repo` threaded from `canonical_repo_name`; `kinds/index_tree.py` threads the same `repo` through `_directory_node`; the schema's link object gained a required `identity` and the `citation` status; the fixture's `alpha.md` gained the two citation links (words 13 to 18, root sum 53 to 58); and `tests/dev_playbook/cloa_viewer/test_identity.py` is new. No deviation from the plan; two facts in Working notes, one of them that `resolve_target` raising on a cross-repo Citation would abort the whole tree if an `index.md` ever listed one — `_link` answers `citation` before it resolves, so only the tree is exposed, and no index here does it. Smoke run on this checkout: 858 `ok`, 20 `external`, 5 `citation`, 45 `broken`, against 711/20/0/195 before; the 45 are real — `~/.claude/...` targets, which stay broken by design, and same-repo Citations of `standards/workflow.md` and `standards/repo-documentation.md`, which do not exist. Gate green, 1291 passed. Next: task 13, the file panel and the Playwright test — the last task, and its `links_out` badges now have four statuses to draw.
